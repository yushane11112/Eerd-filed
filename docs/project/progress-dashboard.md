# 《小耳岛》项目进度仪表盘

更新时间：2026-07-17

## 1. 当前阶段

当前阶段：P1.5 商业级垂直切片收敛期。

当前判断：

### 第一百六十六轮产出（2026-07-17）

- 已将 `public/assets/buildings` 中 28 类建筑的 252 张 L0–L8 RGBA 图接入正式 Pixi 场景：生产组件会按建筑类型和等级解析 `/assets/buildings/<assetId>/level-<level>.png`，使用透明建筑精灵替换对应灰盒层；灰盒仍作为未配置 Provider 的测试/降级路径保留。
- 新增运行时美术契约和缓存 Provider，统一九级路径、等级边界、等距精灵锚点与占地比例；当前生产入口已启用该 Provider。
- 新增运行时资产审计：28 个建筑目录、252 个等级文件、252 个唯一文件，均通过 512×512、RGBA 和 PNG 基础结构检查。
- 这不是商业级美术最终验收：当前仓库仍缺少可追溯 DCC 源文件、模型/骨骼/动画图集与六类金样的完整 manifest，覆盖门禁仍保持红灯。
- 验证：资产运行时审计通过；定向渲染回归 16 项通过；全量 50 个测试文件、330 项测试通过；生产构建 2359 个模块通过；`git diff --check` 通过。

### 第一百六十七轮产出（2026-07-17）

- 已新增独立建筑动效层，使用模拟 tick 和建筑真实状态驱动：施工/升级显示搭架与施工光点；农田/果园/药圃显示随时间摆动的作物/枝叶；码头/渡口/船坊/盐场显示水面波动与作业标记；水车显示旋转轮组；灯塔显示扫光；民居/市集/营业设施显示炊烟和营业活动。
- 动效层按建筑视觉身份分类，不把所有建筑统一成亭子、船或同一套循环；资产 Provider 缺失时仍可运行，动效回退不影响城市模拟。
- 回归中发现并修正旧的“显示层只能有两个子层”硬编码，使测试现在验证动效层存在且对象池数量稳定。
- 验证：运行时资产审计通过；定向动效/渲染回归 16 项通过；全量 50 个测试文件、330 项测试通过；生产构建 2359 个模块通过；`git diff --check` 通过。
- 仍未完成商业级最终动效：当前是代码驱动的差异化运行时动效，尚未替代专业美术制作的骨骼动画、粒子图集、施工过渡片段和完整 DCC 资产。

### 第一百六十五轮产出（2026-07-17）

- 已将全建筑视觉身份目录接入 Pixi 动态渲染：缺少真实 Prefab 时，已映射建筑也会按功能族与等级显示可见身份化灰盒轮廓。
- 当前灰盒已区分民居、农田/果园/茶园、港口/船坊/盐场、灯塔/书院和生产建筑，并随等级改变体量、密度、颜色和功能轮廓标签。
- 这解决了“地图上建筑被隐藏、升级没有可见差异”的运行时断点，但仍不是商业级最终美术；真实 DCC、模型、材质、动画和运行时二进制仍需按金样流水线交付。
- 验证：全量 49 个测试文件、327 项测试通过；生产构建 2358 个模块通过；`git diff --check` 通过。

### 第一百六十四轮产出（2026-07-17）

- 已完成全建筑视觉身份目录：28 类建筑均有独立建筑类别、轮廓族、功能识别、材质语言、动态签名和 L0–L8 视觉成长弧。
- 已把视觉身份接入公共建筑定义和运行时别名；运行时可按等级读取对应视觉阶段，数据层不再允许“不同功能建筑共用同一种终局外形”的设计漏洞。
- 已增加跨资产视觉唯一性门禁，生产 manifest 共享建筑类别、轮廓族或功能识别时会失败。
- 这一轮是视觉设计/运行时契约产出，不是实际美术资产交付；真实 DCC、模型、图集、动画二进制仍未完成，商业美术覆盖门禁继续红灯。
- 验证：全量 49 个测试文件、327 项测试通过；生产构建 2358 个模块通过；`git diff --check` 通过。

- 已建立真实游戏持续预览站：`https://little-ear-island-game-preview.netlify.app`。站点直接发布本仓库的 Vite/Pixi 生产构建，并已绑定 GitHub 自动部署；不是另行制作的宣传落地页。
- 最终总目标不变：稳扎稳打将《小耳岛》做成商业级东方水乡城市文明模拟游戏。
- P0 方向统一、旧原型降级和项目中枢搭建已经完成；当前执行重心从“继续扩规划”转为“把已有系统收敛成可演示垂直切片”。
- 之后每轮优先选择可见、可操作、可验证的主目标，并按 `execution-strategy.md` 的 Tier 0-4 验证分级执行。
- 项目已有早期城市模拟、Pixi 渲染骨架、资产校验器、若干金标 manifest 样例和长稳测试。
- 旧文档中的四岛、听歌材料、固定 28 建筑等主线冲突已在总目标、流水线和门禁中完成降级。
- 本轮已把完整上线商业级目标、美术 ART-P0–P11 全阶段生产线和自动执行机制落到项目中枢。
- 第十二轮已把外来人口从直接生成住户推进为候选状态机，并建立第一版城市吸引力评分。
- 第十三轮已让等待中的外来人口在地图和小事流中可见、可定位。
- 第十四轮已建立建筑阶段、功能、连接方式和年代标签的运行时分类目录。
- 第十五轮已完成街区繁荣第一版：街区数据、指标和地图暖光/灯点/人流轻表现已接入。
- 第十六轮已把候选外来人口从瞬时入住推进为可见进城路径：等待、移动、抵达住宅后才正式入住。
- 第十七轮已把建筑目录的城市阶段接入建造菜单：当前阶段可见，未解锁建筑置灰，运行时放置也会被拒绝。
- 第十八轮已把候选外来人口进城路径从直线推进为道路优先寻路，有路时不会横穿地块。
- 第十九轮已把阶段晋升条件做成可见目标：当前阶段、下一阶段和人口/吸引力/街区要求能在界面上看到。
- 第二十轮已把听歌奖励入口从常驻主线卡片降级为默认收起的可选轻奖励入口，继续清理旧方向残留。
- 第二十一轮已把旧群岛引擎里的“听歌刷普通材料”默认关闭，只保留显式 legacy 兼容开关。
- 第二十二轮已把旧 `IslandCanvas` 群岛 Pixi 视图默认归档，只有显式 legacy/QA 参数才会启动旧渲染。
- 第二十三轮已把阶段晋升条件做成可点击顾问：人口、吸引力、街区缺口会引导到下一步操作。
- 第二十四轮已给阶段顾问接入原因诊断：住房、岗位、满意度、物流和街区卡点会直接显示。
- 第二十五轮已把阶段顾问接入地图覆盖：点击后可看到住房/外来人口、吸引力瓶颈或街区核心位置。
- 第二十六轮已把阶段覆盖升级为分层标记：住房、外来人口、瓶颈和街区核心有独立类型、颜色和短标签。
- 第二十七轮已在阶段面板加入覆盖图层开关：住房容量、服务覆盖、物流拥堵和道路连通可按需查看。
- 第二十八轮已让物流图层显示订单线路：发货点、收货点和方向线能同时看到。
- 第二十九轮已让服务图层显示服务范围：服务点之外可以看到半透明覆盖区域。
- 第三十轮已让服务图层标出未覆盖住宅：玩家能看到哪些民居需要补市场/服务点。
- 第三十一轮已让道路图层按建筑功能细分缺路入口：住宅、服务、仓储和生产缺口更容易分辨。
- 第三十二轮已让物流图层标出多订单热点：发货/收货线路之外能看到拥堵集中建筑。
- 第三十三轮已给覆盖图层增加摘要条：住宅/空位、服务缺口、物流热点和道路缺口可直接读数。
- 第三十四轮已给覆盖图层增加结构化指标：后续瓶颈/阶段面板可以直接读取图层数值。
- 第三十五轮已把图层结构化指标接入阶段面板：当前打开图层的治理读数可直接看到。
- 第三十六轮已让阶段面板图层指标可点击定位：读数不再只是摘要，可以直接跳到对应地图目标。
- 第三十七轮已把旧群岛 `IslandCanvas` 从正式组件目录迁入 `src/legacy/archipelago`，并新增 legacy 边界测试，防止旧原型继续污染正式 UI 搜索路径。
- 第三十八轮已抽出共享移动路径服务：候选外来人口不再在 `SimulationEngine` 内部自带寻路实现，而是使用 `src/simulation/world/movementPath.ts` 的道路优先路径，后续居民/物流可逐步共用。
- 第三十九轮已把图层结构化指标接入瓶颈治理卡：服务缺口、道路入口缺口和物流热点会生成可排序、可点击、可定位的治理建议。
- 第四十轮已让街区繁荣接入真实运行因子：服务覆盖、道路贴近和物流活动会提高街区繁荣与人流表现，繁荣不再只看建筑聚集。
- 第四十一轮已让物流 RoadRoutePlanner 复用共享移动路径服务：货运不再穿越普通水面或建筑占用格，桥路可通行，无有效道路时会保持物流失败而不是自动直线兜底。
- 第四十二轮已把共享移动路径接入工人通勤：工人获得岗位后会生成道路优先路径，逐 tick 从住宅入口移动到雇主入口，抵达后才进入 working 状态。
- 第四十三轮已补上工人返家循环雏形：工人完成固定工作班次后生成返家路径，沿共享道路回到住宅入口并恢复 home 状态；这还不是完整日夜作息，只是生活循环的第一段闭环。
- 第四十四轮已把市场/服务成功转化为可见居民出行：服务交付时生成 `service-visit` 居民 agent，沿住宅到服务建筑路径前进，到达后返家并移除；同一家庭同一需求不会重复刷出多个人影。
- 第四十五轮已把居民服务/购物从即时结算推进为抵达后结算：居民到达市场、药铺、书院或戏台后才扣库存、扣家庭收入、产生税收和恢复需求；长稳压力场景明确 `migrationOutThreshold: 0` 为禁用迁出，并按 500 户满规模重新校准历史订单灰盒上限。
- 第四十六轮已把真实服务访问接入街区繁荣热度：居民正在前往市场、药铺、书院或戏台时，对应市街/服务街区会获得繁荣与人流加成，地图人流不再只由静态建筑和物流推导。
- 第四十七轮已把治理卡推进为可执行城市顾问：服务缺口、道路缺口、物流热点和常规瓶颈会显示原因、推荐工具/建筑和相关图层，玩家可一键定位或切换到铺路/营造工具。
- 第四十八轮已给承运人补上货运生命周期语义：货车/船只 agent 会记录订单、资源、数量、源/目的建筑和取货/送货阶段，动态渲染也能区分空车取货与载货送达。
- 第四十九轮已完成历史物流订单归档：已交付/已取消订单会进入 `logisticsArchive` 汇总，主快照只保留最近完成记录；2400 tick 长稳订单表上限从 20000 收紧到 2000，并验证归档确实发生。
- 第五十轮已把旧群岛材料/建造/存档引擎和旧材料 UI 迁入 `src/legacy/game/**`，正式 `src/game` 与旧 `MaterialRow` 已移除。
- 第五十一轮已新增城市活动热力图层：服务访问、工人通勤和货运路线会汇总为活动热力点、活动路径和基础活动指标。
- 第五十二轮已把活动热力推进为治理读数：道路压力、服务热度和货运拥堵会进入阶段面板，并在压力集中时生成“城市活动压力”治理卡。
- 第五十三轮已把活动压力投射到真实道路格：有路网时会读取 agent 剩余路径并生成 `货路/服路/通路/道压` 承压点，阶段面板“道压”可定位到具体道路段。
- 第五十四轮已把活动压力治理卡推进为分因推荐：货拥推荐补仓储缓冲，服务热推荐补服务点分流，道压推荐铺路分流。
- 第五十五轮已把治理卡建筑推荐接入城市阶段门禁：推荐对象会说明当前阶段和所需阶段，未解锁建筑会降级为查看图层，UI 点击推荐时也会二次拦截。
- 第五十六轮已把已解锁建筑推荐接入营造可执行性诊断：会检查连续空地和入口道路，无法放置时显示原因且不切换到建筑工具；当前系统尚未有正式营造材料/财政成本，因此不伪造资源门禁。
- 第五十七轮已建立首版正式营造成本：建筑放置会消耗财政和城市仓储材料，资源不足时不落建筑、不扣银两；治理卡营造条件也会诊断材料/银两不足。当前数值是运行时首版平衡，不等于最终商业经济表。
- 第五十八轮已把正式营造成本接入建造菜单：按钮显示银两/材料成本，资源不足时显示缺口并禁用营造入口。
- 第五十九轮已把建筑类治理建议接入地图落点提示：点击推荐会在图层中优先显示“建议落点/入口”，并在建造菜单标出推荐建筑；浏览器 QA 发现并修复了建议点被热点裁掉的问题。
- 第六十轮已把建筑类治理建议从点提示推进为 footprint 预览：推荐会输出真实建筑占地、入口格和旋转占位字段，地图显示 3×2 等距占地格与入口高亮，浏览器 QA 验证集市推荐出现 6 个 footprint 格、1 个入口格、2 个落点标记。
- 第六十一轮已把建筑工具接入动态试放预览：选择建筑后，鼠标移动到地图会显示 footprint、入口和冲突格；预览由 `GameRuntime.previewBuildingPlacement` 统一校验，不扣资源、不落建筑，浏览器 QA 验证了“入口必须紧邻道路”的冲突提示。
- 第六十二轮已把动态试放接入 `PlacementController`：选择、移动、旋转、确认、取消和成功后的重复放置改由状态机驱动；新增 Runtime placement 适配层，浏览器 QA 验证选择民居、移动预览、R 旋转保留预览、右键取消清空预览均正常。
- 第六十三轮已把正式营造成本接入试放预览：地块和道路条件满足但城市仓储/银两不足时，`GameRuntime.previewBuildingPlacement` 会直接返回不可营造和缺口，UI 预览因此在规划阶段变红，而不是点击后才失败。
- 第六十四轮已把道路工具从单击铺路推进到拖拽连续铺设：`GameRuntime.placeRoadPath` 能一次批量修改真实路网并汇总建筑占用/地形/越界/无变化跳过格，`SimulationCanvas` 在道路工具拖拽时调用该入口而不再平移相机。
- 第六十五轮已新增“拆路”工具：`GameRuntime.removeRoadPath` 能批量删除真实道路并汇总无路/越界跳过格，UI 侧提供明确拆路入口；当前只拆道路，不拆建筑，避免破坏人口、库存和物流一致性。
- 第六十六轮已给道路铺设接入财政成本：石板路每格消耗银两，拖拽路径会铺到财政可支付的部分，余额不足的格子跳过；拖过已有道路不会重复扣费或误判为缺钱。
- 第六十七轮已建立建筑拆除一致性入口：建筑详情可拆除建筑，运行时会释放地块、迁出失去住宅的家庭、释放相关岗位并取消引用该建筑的未完成物流订单。
- 第六十八轮已新增桥梁专门建造模式：桥梁工具可拖拽水面/岸边架桥，桥路按 18 银两/格扣费，普通陆地会被明确拒绝，水乡城市开始具备跨水交通规划能力。
- 第六十九轮已给桥梁接入专属道路视觉：桥路不再完全复用普通石板路样式，而是有独立桥面色、描边、桥墩和更宽桥面；这只是灰盒视觉差异，不等于商业级桥梁资产完成。
- 第七十轮已把道路图层从“入口旁有没有路”推进到“是否连到主路网”：建筑贴着孤立道路时会被标为未连通，阶段面板也会显示未连通和孤立路网读数。
- 第七十一轮已把“未连通/孤立路网”接入城市治理卡：当道路或桥梁只铺到局部时，瓶颈面板会优先提示“道路未连通”，而不是只当成普通缺路或物流不畅。
- 第七十二轮已让道路图层输出建议补线：孤立路网会找到离主路网最近的道路边界，并按是否跨水标出“建议接路”或“建议补桥”路径。
- 第七十三轮已把道路/桥梁补线建议推进为施工计划：治理卡会拆出需要新铺的中间格，区分石板路和桥梁，给出银两成本、缺口和当前是否可支付。
- 第七十四轮已把补线施工计划接入治理卡 UI 与地图覆盖：瓶颈卡可读出道路/桥梁格数、预计银两和财政缺口，点击推荐时保留 roadPlan 并高亮待施工格。
- 第七十五轮已把补线施工计划推进为一键施工：运行时可一次执行混合道路/桥梁 roadPlan，逐格处理财政、地形、建筑占用、已有道路和缺钱反馈，UI 点击 roadPlan 治理卡会直接施工。
- 第七十六轮已补上可控孤立路网调试/E2E 场景：URL 参数可稳定制造道路未连通卡，浏览器验证 roadPlan 卡出现、点击一键施工、财政扣费、成功 toast 和补线 overlay 清理。
- 第七十七轮已补上补线财政不足调试/E2E 场景：URL 参数可稳定制造不可支付 roadPlan，浏览器验证缺钱文案、点击失败不扣钱、保留施工格提示和手动铺路模式。
- 第七十八轮已把道路补线成功/失败场景沉淀为可重复 QA 命令：`npm run qa:road-plans` 会输出并校验两个 roadPlan 场景的前置指标、执行结果和后置指标。
- 第七十九轮已把服务缺口治理纳入 QA 场景库，并修复服务推荐“可建但无效”的问题：市场推荐会优先选择建成后能减少服务缺口的落点，`npm run qa:service-governance` 固化该链路。
- 第八十轮已把桥梁缺口治理纳入 QA 场景库：`bridge-gap` 调试场景能稳定制造水面断点，治理卡会生成 2 格桥梁 roadPlan，`npm run qa:bridge-gaps` 固化“补桥后孤立路网下降”的链路。
- 第八十一轮已把物流热点治理纳入 QA 场景库：`logistics-hotspot` 调试场景能稳定制造 3 条未完成订单压向同一市场，治理卡优先定位入货端，并由 `npm run qa:logistics-hotspots` 固化。
- 第八十二轮已建立浏览器 E2E 场景契约：`npm run qa:browser-e2e:contract` 会列出并校验 roadPlan、低财政、bridge-gap、logistics-hotspot、service-governance 五个浏览器场景的 URL、可见文案和 console error 门禁；这仍不是实际浏览器点击执行器。
- 第八十三轮已接入真实浏览器 E2E 执行器：`npm run qa:browser-e2e` 会构建生产包、启动 Vite preview、用 Playwright Chromium 打开 5 个治理场景、检查可见文案和 console error，并在 road-plan-success 场景真实点击治理卡验证 toast。
- 第八十四轮已补上低财政失败路径真实浏览器交互：`road-plan-low-treasury` 会点击“打开道路图层并接回主路网”，并验证 toast 显示“银两不足2，无法执行补线施工”。
- 第八十五轮已补上服务治理真实浏览器交互：`service-governance` 会点击“打开服务图层并营造市场”，并验证 toast 显示“服务覆盖缺口：打开服务图层并营造市场。”。
- 第八十六轮已补上桥梁缺口真实浏览器交互：`bridge-gap` 会点击“打开道路图层并接回主路网”，并验证 toast 显示“道路未连通：补线施工完成：桥梁 2 格，花费银两36。”。
- 第八十七轮已补上物流热点真实浏览器交互：`logistics-hotspot` 会点击“打开物流图层并补仓储”，并验证 toast 显示“物流热点拥堵：打开物流图层并补仓储。”；至此 5 个固定治理浏览器场景都具备真实点击断言。
- 第八十八轮已建立 7200 tick/30 日分层长跑 QA：`npm run qa:civilization-long-run` 会在 2400、4800、7200 tick 采样人口、满意度、物流、停工、订单、库存、归档和主快照表规模；本轮实测最终订单表 539、归档 53462、无非法数值。
- 第八十九轮已把营造成本从私有硬编码推进为可导出、可校验、可替换的 `DEFAULT_CONSTRUCTION_ECONOMY_TABLE`，并新增经济表与平衡审计文档；默认数值保持不变。
- 第九十轮已把服务容量从隐含扣分推进为显式 `serviceQueues` 快照状态：服务建筑每 tick 吞吐、多余家庭排队、等待 tick 和排队热点可被模拟、治理图层和后续 UI 读取。
- 第九十一轮已把物流热点从“订单数量”推进到“目的建筑卸货吞吐”：同 tick 多车到达同一建筑时会受 `unloadCapacityPerTick` 限制，未卸货订单保留在目的地并形成 `logisticsQueues` 积压状态，物流图层显示 `卸货排队xN`。
- 第九十二轮已把物流治理卡从“统一补仓储”推进为分因建议：无车、断路、仓满、来源不足、卸货排队会分别给出调度、修路、扩仓、补生产或分流卸货建议。
- 第九十三轮已把建筑升级成本纳入 `ConstructionEconomyTable.upgradeCosts`：默认曲线保持旧平衡，自定义表可影响升级报价和城市仓储扣料，校验器会拒绝非法升级参数。
- 第九十四轮已把 7200 tick 长跑报告接入 `queuePressure`：每个 2400/4800/7200 层都会记录服务队列、排队家庭、最长服务等待、物流卸货队列和卸货积压；`npm run qa:civilization-long-run` 额外执行真实物流卸货吞吐探针，避免物流队列模型只停留在单测里。
- 第九十五轮已把物流卸货压力推进到 7200 tick 主长跑：新增 QA 负载系统定期注入真实在途订单和货车，由 `LogisticsSystem` 按 `unloadCapacityPerTick=1` 形成主报告中的卸货积压；本轮实测最终 `logisticsQueues=2`、`unloadBacklog=2`、agent 表 208 且无非法数值。
- 第九十六轮已把物流分因治理从纯文案推进为结构化执行计划：缺车、断路、仓满、缺货源、卸货排队会输出 `logisticsPlan`，包含计划类型、订单样本、来源建筑、目的建筑和资源，治理卡 UI 会显示计划摘要。
- 第九十七轮已把结构化物流执行计划推进为可聚焦的运行时入口：计划会声明下一步聚焦来源、目的地、线路或缓冲仓，并提供可定位建筑；治理卡“定位”和 inspect 类执行会优先选中真实相关建筑。
- 第九十八轮已把物流断路执行计划接入可施工 roadPlan：`build-road-link` 会根据订单来源/目的建筑入口生成道路/桥梁施工格、银两成本和可支付状态，并复用现有治理卡一键施工链路。
- 第九十九轮已把物流仓储类执行计划接入营造落点候选：`expand-storage`、`split-unload` 和 `add-buffer-storage` 会在通过阶段/资源校验后，优先给出贴近物流热点建筑且入口邻路的粮仓 footprint，统一可用性校验也会保留这种定制 execution。
- 第一百轮已把物流执行计划接入建筑详情面板：定位来源/目的建筑后可看到计划相关库存、订单数、忙碌承运和待派订单。
- 第一百零一轮已把承运调度计划接入运行时动作：治理卡可释放关联承运人并把订单重置回等待重新派车。
- 第一百零二轮已把来源库存计划接入运行时调拨动作：治理卡可从备用库存调入缺货源建筑并重置关联订单。
- 第一百零三轮已把缺承运计划接入正式容量补充动作：治理卡可新增一名货车承运人，并由正式物流系统派单。
- 第一百零四轮已把金标占位视觉 registry 接入主画布：民居、粮仓、集市和码头的程序化细节层从测试链路进入真实游戏画面。
- 第一百零六轮已完成 starter 建筑第一眼改观：民居、粮仓、集市、码头和稻田拥有更强的程序化轮廓层，稻田默认 registry 也接入主画布。
- 第一百零七轮已完成道路与街区质感增强：土路车辙、石路板缝、桥面木板、水波、岸线和田地纹理进入主画布。
- 第一百零八轮已完成活动密度与状态反馈增强：agent 渲染新增稳定的活动尾迹、主体和标记层，通勤、服务访问、取货、送货和工作状态更容易区分。
- 第一百零九轮已把物流扩仓/分流卸货计划接入真实运行时动作：`expand-storage`、`split-unload`、`add-buffer-storage` 可用治理卡候选落点直接建成粮仓，支付营造成本，重置关联订单并清空卸货队列。
- 第一百一十轮已完成卸货能力分层：正常物流不再使用单一全局卸货能力，而是按目的建筑类型、等级、工人和入口道路数动态计算卸货吞吐；QA 压力场景仍可显式覆盖能力值。
- 第一百一十一轮已补上来源库存治理浏览器场景：`logistics-source-shortage` 可稳定复现来源库存短缺，真实点击“检查来源库存”后会定位粮仓并验证建筑详情中的物流执行计划面板可见。
- 第一百一十二轮已把升级成本从统一倍率推进为 `defaultCurve` / `categoryCurves` / `typeCurves` 三层曲线；住宅、仓储、生产、市场、服务、港口和地标等类别可配置不同材料与里程碑成本，运行时升级报价和扣料已接入建筑定义。
- 第一百一十三轮已补上物流仓储建造真实浏览器场景：`logistics-storage-build` 稳定制造卸货排队，真实点击“分流卸货压力”后会建成粮仓缓冲、扣除营造成本并重置关联物流订单。
- 第一百一十四轮已完成卸货能力可视化解释：物流队列记录能力来源拆解，治理卡和建筑详情面板会说明基础、等级、工人和入口道路如何形成当前卸货能力，并把 `logistics-storage-build` 浏览器场景校准为真实建筑吞吐口径。
- 第一百一十五轮已完成物流仓储建造后状态解释：建成粮仓会把订单重置、承运人释放和卸货队列清理结果写入运行时快照，自动选中新仓详情并持续显示结果，浏览器场景已验证不依赖 toast。
- 第一百一十六轮已完成升级经济回本与容量联动审计：覆盖 5 类运行时建筑、40 个升级节点；服务吞吐和生产建筑里程碑产出/周期均已真实联动，报告继续暴露部分低等级节点回本不足的平衡风险。
- 第一百一十七轮已把升级经济结果接入建筑详情：玩家可看到本级容量/岗位、生产产值、服务吞吐、维护影响和回本周期；目标测试与生产构建通过，浏览器复验因本地 4173 预览服务未响应而待补。
- 第一百一十八轮已把最高风险升级节点接入城市顾问：顾问只显示一条最值得注意的升级经济风险，点击后定位建筑详情，不平铺复杂经济表；目标构建与测试通过，浏览器预览服务复验仍待补。
- 后续重点继续从“规划统一”转向“代码主循环与运行时系统重构”。

## 2. 总体完成度

| 维度 | 当前状态 | 商业级完成度 |
| --- | --- | --- |
| 项目规划 | 完整上线总计划、阶段产出、生产线、自动执行和产物记录机制已建立 | 45% |
| 城市模拟 | 有早期生产、物流、市场、升级、财政、道路优先候选迁入、共享移动路径、物流路径复用、承运人货运生命周期、承运容量补充动作、承运重新调度动作、来源库存调拨动作、物流仓储建造动作、历史订单归档、目的建筑分层卸货吞吐/积压快照和能力来源拆解、物流干预结果持久化、升级经济回本审计、服务升级吞吐联动、生产建筑里程碑产出/周期联动、7200 tick 主长跑卸货压力、工人上下班路径、服务访问抵达后结算、服务容量/排队快照、服务访问驱动的街区繁荣、道路格活动压力治理、物流热点入货端定位、分因推荐、阶段门禁解释、营造可执行性诊断、可调营造/道路/分类型升级经济表、道路财政成本、建筑拆除一致性和试放资源门禁部件，但正式经济闭环与治理闭环仍不足 | 62% |
| 地图建造 | 有等距地图、道路、桥梁、放置、寻路基础、道路连通诊断、孤立路网补线建议、物流断路补线计划、物流仓储候选落点与真实建造动作、补线施工格/成本计划、overlay 高亮、一键施工、可控道路 E2E 场景和桥梁缺口真实浏览器交互，营造成本、推荐 footprint 预览、建筑工具动态试放预览、资源不足预览红态、PlacementController 主流程接入、道路/桥梁拖拽连续建造、道路/桥梁基础视觉区分、道路拆除、道路铺设财政成本、道路铺装纹理和建筑详情拆除入口；仍缺桥头吸附、批量规划、高级旋转反馈和拆除确认/回收平衡 | 53% |
| 动态引擎 | 有 Pixi 骨架、状态占位、主画布默认金标占位 registry、民居/粮仓/集市/码头/稻田程序化轮廓和细节层、土路/石路/桥面铺装纹理、水波、岸线、田地纹理、候选人口移动、工人上下班移动、服务访问移动、承运人取货/载货状态、agent 活动尾迹/标记、城市活动热力/压力图层、道路格承压点、推荐/试放 footprint overlay 和受服务/道路/物流/居民访问影响的街区繁荣轻表现，正式状态驱动动画不足 | 37% |
| 建筑体系 | 可扩展建筑与街区体系已完成设计，运行时分类目录、阶段解锁菜单、建筑类型/等级驱动卸货能力、分类型升级成本和街区繁荣第一版已接入 starter 建筑 | 36% |
| 人口生命周期 | 设计已冻结，候选外来人口抵达/等待/沿共享路径移动/入住/离开已落地，工人上下班路径和服务访问抵达后结算已接入，但休闲、家庭日程和职业身份细分还不完整 | 35% |
| 美术资产 | ART-P0–P11 全阶段计划和门禁已建立，main-homes/main-eatery/main-pier 样例、main-granary 临时占位 descriptor 与 windfield-rice 稻田占位 descriptor 已进入主画布，starter 建筑轮廓更可辨，正式商业资产仍不足 | 14% |
| UI/UX | 有基础面板和交互，主画布可见金标占位细节，吸引力指标、迁入小事、阶段解锁菜单、阶段目标、可点击顾问、升级经济风险定位、原因诊断、可切换地图覆盖图层、图层摘要/面板指标、道路未连通/孤立路网读数与治理卡、补线施工计划摘要、待施工格高亮和一键施工反馈、服务缺口有效市场落点推荐、物流仓储热点候选、物流仓储计划一键建造、物流执行计划详情、卸货能力来源解释、仓储建成后物流结果持久化展示、升级经济结果和回本提示、承运容量补充反馈、承运重新调度反馈、来源库存调拨反馈、服务排队热点指标、卸货排队热点指标、物流分因治理建议、结构化物流执行计划、计划聚焦目标、可点击图层指标、物流线路、物流热点、服务范围、服务缺口、道路缺口、活动热力/压力图层、道路格承压定位、分因推荐、阶段门禁解释、营造条件/资源不足提示、建造菜单成本、推荐落点/footprint 预览、资源不足试放红态、状态机驱动建筑试放、道路拖拽铺设、道路拆除和可执行治理卡已可见，旧听歌入口/旧群岛视图已降级归档并迁入 legacy 边界，城市管理信息层级仍需重构 | 81% |
| QA 性能 | 有单测、构建、2400 tick 长稳测试、7200 tick/30 日分层长跑命令、服务队列/物流队列压力读数、主长跑物流卸货压力、物流卸货吞吐探针、历史订单归档验证、升级经济回本审计命令、`qa:road-plans`、`qa:service-governance`、`qa:bridge-gaps`、`qa:logistics-hotspots` 可重复场景命令，`qa:browser-e2e:contract` 浏览器场景契约，以及 `qa:browser-e2e` 真实 Chromium 场景执行；7 个固定治理浏览器场景均具备真实点击/可见断言，更多长期性能 E2E 和商业级规模基准仍不足 | 46% |
| 发布运营 | 远未进入上线准备 | 5% |

> 完成度保守估计；文档完成不等于系统完成。

## 2026-07-17 第一百一十九轮补充

- 物流图层在卸货排队热点旁显示能力来源短标签：能力总量及基础、等级、工人、入口道路贡献均可在场景内快速识别；UI/UX 完成度调整为 82%。
- 定向测试、全量测试、生产构建和差异检查通过；真实浏览器验收仍等待允许本地预览服务监听的环境。

## 2026-07-17 第一百二十轮：物流干预事件与存档审计

- 扩仓治理动作现在写入 `logisticsStorageInterventionHistory`：每条记录包含唯一事件、建设建筑、发生刻数、涉及订单和真实重置/释放/清理计数；旧的按建筑结果字段继续保留兼容 UI。
- 新增 `npm run qa:logistics-storage-audit` 和审计单测，检查重复事件、失效建筑、非法计数及累计结果，保证运行时重建后仍能从快照追溯。
- 模拟核心完成度调整为 63%，QA 性能调整为 47%；全量测试、生产构建和差异检查通过。

## 2026-07-17 第一百二十一轮：物流干预历史可见化

- 建筑详情新增“物流干预记录”卡，展示同一缓冲建筑的多次干预累计结果和最近事件刻数；后台历史不再是不可见数据。
- UI/UX 完成度调整为 83%；定向测试和生产构建通过，待全量回归及允许本地监听环境下的真实浏览器验收。

## 2026-07-17 第一百二十二轮：物流干预历史有界归档

- 物流干预明细历史限制为最近 200 条，超出部分进入累计归档；归档保留事件数、重置订单、释放承运、清理队列和最后刻数，长跑不会因干预事件无限增长。
- 新增 205 条事件边界测试，验证保留窗口、首尾事件和归档累计；审计器现在同时统计明细与归档。
- 模拟核心完成度调整为 64%，QA 性能调整为 48%；定向测试、全量 280 项测试、生产构建和差异检查通过。

## 2026-07-17 第一百二十三轮：物流干预归档接入城市管理面板

- 城市管理“瓶颈”抽屉新增治理存档卡，显示近期明细数量、已归档数量、累计重置订单、释放承运和清理队列；归档不再只存在于后台快照和 QA 输出中。
- 新增 `formatLogisticsStorageArchive` 纯函数及单测，统一城市管理面板的归档统计文案，并在存在归档时显示最近归档刻数。
- 定向 38 项测试、全量 280 项测试、生产构建和差异检查通过；真实浏览器验收仍受当前环境禁止本地 socket 监听影响。

## 2026-07-17 第一百二十四轮：物流治理时间线可定位

- 城市管理抽屉新增最近治理动作时间线，展示最近三次物流干预的刻数、建筑和真实结果；没有事件时显示轻量引导，不制造空面板噪声。
- 每条时间线记录可点击，复用既有建筑定位、相机聚焦和详情抽屉链路；已移除建筑会明确显示为历史记录，不伪造定位目标。
- 新增时间线文案单测；定向 38 项、全量 281 项测试、生产构建和差异检查通过，真实浏览器验收仍待解除本地监听限制。

## 2026-07-17 第一百二十五轮：城市运行时间线接入文明事件

- 新增 `cityTimeline` 快照历史，接入公共服务完成、市场交易/税收、新家庭入住、家庭离开、外来人口抵达和人口候选离开六类文明运行事件。
- 时间线限制最近 200 条，事件记录包含来源类型、发生刻数、可读详情和可选建筑目标；服务、财政事件可点击定位相关建筑，人口事件保留城市层级事实。
- 城市管理抽屉新增“城市运行”区块，与物流治理时间线分层展示；定向 51 项、全量 284 项测试、生产构建和差异检查通过。

## 3. 当前最大风险

- 节奏失焦风险：过往大量改动集中在系统、物流、治理和 QA，工程基础增强明显，但玩家第一眼视觉变化不足。已通过 `execution-strategy.md` 校准为“可见垂直切片优先”。
- 旧方向残留：旧听歌掉落、旧群岛视图和旧固定岛屿材料/建造/存档引擎均已迁入 `src/legacy/**`；正式 `src/game` 与旧 `MaterialRow` 组件已移除，后续只需防止旧原型回流主线。
- 美术产能风险：商业级美术需要专业二维/三维/动画参与，工程只能先建立规范、灰盒、manifest 和金标样板。
- 模拟闭环风险：迁入已受吸引力约束，但就业、服务、财政、风险之间还没有完全形成长期自洽闭环。
- 视觉密度风险：starter 建筑、道路地表和 agent 活动标记已经连续增强，但仍是程序化占位；物流扩仓/分流卸货、卸货能力分层、卸货能力解释、分类型升级经济、升级回本审计、生产里程碑产出和仓储建造浏览器回归已接入真实动作，后续要继续把这些经济结果投射到可见反馈。

## 4. 下一轮默认任务

1. 修复/恢复本地预览服务并完成升级经济顾问与物流图层真实浏览器验收。
2. 将城市运行时间线接入真实浏览器场景和居民身份/职业变化，并继续扩大文明系统的可见运行反馈。
3. 每轮按 `execution-strategy.md` 验证分级执行：视觉轮至少目标测试、开发构建和截图；模拟核心轮必须全量测试和生产构建。

## 2026-07-17 第一百二十六轮：居民入住前后身份与职业状态可见化

- 城市运行人口事件新增居民状态快照：外来/候选/离城家庭、成员数、劳动力数、已就业数、职业建筑、住房和满意度；数据来自真实 `SimulationSnapshot`，不是静态 UI 文案。
- 城市管理时间线在“新家庭入住”“候选家庭抵达”“家庭离城”记录下显示身份和就业状态，入住记录可继续定位到真实住房建筑。
- 新增 `civilization-resident-timeline` 可重复浏览器场景契约，覆盖候选家庭“待安置”到外来家庭“已入住”的可见差异；当前环境仍无法取得真实浏览器点击证据。
- 定向 49 项测试、全量 285 项测试、生产构建和 `git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 在允许本地 socket 的环境补跑 `civilization-resident-timeline` 真实浏览器场景，并保留截图/日志证据。
2. 将居民职业状态继续接入工作路线、住房状态和服务消费反馈，避免只在时间线中显示一次。
3. 继续按 `execution-strategy.md` 推进文明系统的可见垂直切片，再扩大模拟闭环。

## 2026-07-17 第一百二十七轮：居民生活状态进入城市运行反馈

- 新增 `residentGovernance` 派生层，从真实家庭、住房、工人代理、雇佣建筑、活动和民需状态计算居民生活摘要，不复制一套脱离模拟的居民数据。
- 城市管理抽屉新增居民生活卡，显示居住容量、就业比例、迁入候选、平均民需、最低民需、当前活动、职业分布和拥挤家庭；住房详情显示真实入住人数。
- 动态地图中的工人按所属生产、市场、服务、仓储、港口或地标职业类别使用不同服色，通勤/工作/返家动画仍由真实 `AgentEntity.activity` 驱动。
- `civilization-resident-timeline` 浏览器契约已扩展到同时检查“居民生活”和“就业”文本，确保居民治理卡有可执行的验收入口。
- 新增居民治理派生与格式化测试；定向 28 项测试、生产构建、全量回归和 `git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将职业类别进一步接入建筑运行表现：职业工人缺勤、岗位变更和生产/服务结果要在同一居民链路中可追溯。
2. 在允许本地 socket 的环境补跑居民生活真实浏览器场景，并取得截图/日志证据。
3. 继续推进家庭生命周期和阶层/身份差异，扩大到教育、服务访问和迁出原因的可见闭环。

## 5. 每轮更新要求

每轮完成后必须按变更风险更新：

- 本仪表盘的阶段、完成度、风险和下一轮任务。
- `HANDOFF.md` 的跨电脑接续口径，若下一轮默认任务或执行节奏改变。
- `task-board.md` 的任务状态，若新增或完成可追踪任务。
- `integration-log.md` 的集成记录，若有代码、画面、测试或产品事实变化。
- `artifact-index.md` 的真实文件产出。
- `qa.md` 的验证等级、测试结果或跳过原因。

## 2026-07-17 第一百二十八轮：岗位与居民缺勤形成真实运行后果

- 新增 `worker-employment-changed` 与 `worker-attendance-changed` 事件：居民获得/失去岗位、因健康或满意度过低缺勤、恢复后重新进入工作路线，均来自模拟状态变化。
- 将“岗位人数”与“有效出勤人数”拆分：生产、公共服务、物流卸货能力和劳动力告警现在按实际出勤工人计算；缺勤仍保留原岗位，不会被错误当作迁出或永久失业。
- 城市运行时间线新增“劳务”分类，展示岗位变更和缺勤原因；居民治理卡新增缺勤人数，形成居民状态到建筑运行的可追溯链路。
- 兼容旧存档和测试夹具：缺少工人实体的历史岗位 ID默认视为有效，只有显式 `workStatus: absent` 才降低产能。
- 目标 GREEN：全量 46 个测试文件、289 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将缺勤和岗位变更继续接入生产里程碑、服务覆盖和迁出原因，形成可解释的长期反馈。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，取得城市运行、居民生活和缺勤状态截图/日志。
3. 继续推进家庭生命周期、教育和阶层差异的可见垂直切片。

## 2026-07-17 第一百二十九轮：居民迁出原因可解释化

- `household-migrated` 的离城事件新增原因：关键需求长期无法满足、长期失业、长期满意度过低；原因由真实需求、就业和满意度状态确定，不由 UI 猜测。
- 城市运行时间线将离城记录改为可读解释，玩家可以区分服务/需求压力与就业压力造成的人口流失。
- 保留旧存档兼容：迁移历史中没有原因的旧记录默认展示为一般性低满意度，不破坏已有时间线。
- 目标 GREEN：全量 46 个测试文件、289 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将迁出原因继续细分到具体服务需求和缺勤累计，形成从居民状态到人口变化的长期因果链。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，取得居民生活、缺勤和迁出原因截图/日志。
3. 继续推进教育、阶层和城市阶段差异的可见运行反馈。

## 2026-07-17 第一百三十轮：需求短板与长期缺勤持久化

- `HouseholdState` 新增 `unmetNeedTicks` 与 `absenceTicks`：每项需求在低于关键阈值时累计压力，家庭存在缺勤工人时累计缺勤刻数，并随存档保留。
- 离城事件新增 `need` 与 `absenceTicks` 解释字段，时间线能明确显示“医疗/粮食等具体需求长期不足”或“连续缺勤 N 刻”，不再只显示笼统的生活压力。
- 新增长期缺勤迁出测试，验证需求已稳定时仍能由持续缺勤触发离城原因；旧事件和旧存档字段继续兼容。
- 目标 GREEN：全量 46 个测试文件、291 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将具体需求压力继续绑定到服务覆盖与生产资源来源，形成“缺什么服务/资源 → 哪类需求下降 → 居民后果”的可审计链路。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，取得居民生活、缺勤和迁出原因截图/日志。
3. 继续推进教育、阶层和城市阶段差异的可见运行反馈。
## 2026-07-17 第一百三十一轮：服务短缺进入居民因果链

- 服务系统现在为家庭的每项未满足需求记录来源：服务点无有效工人、服务库存缺资源、道路不可达、服务容量不足或家庭收入不足，并累计持续刻数。
- 服务压力来自真实的服务队列、工人出勤、库存、道路和家庭现金状态；不是 UI 推断。服务恢复后，对应需求压力会清除。
- 居民治理卡显示当前持续时间最长的服务短板；迁出时间线会显示具体需求、瓶颈类型和关联服务建筑。
- 目标 GREEN：全量 46 个测试文件、292 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 把服务压力继续扩展到“无服务设施”这一城市级状态，并让治理顾问能定位新建/扩容的真实候选建筑。
2. 在允许本地 socket 的环境补跑居民治理、服务短板和迁出原因浏览器场景，取得截图/日志证据。
3. 继续推进教育、阶层和城市阶段差异的可见运行反馈。

## 2026-07-17 第一百三十二轮：城市级缺失服务设施治理

- 新增城市级服务覆盖分析：当家庭某项需求平均满足度低于 70%，且城市没有对应服务设施时，治理顾问会生成独立的“缺少服务设施”卡片。
- 缺失设施卡沿用现有服务图层和治理动作协议：可定位到需求最弱家庭的住宅入口；已有市场定义时给出真实营造候选，尚未进入当前时代建筑定义的医疗/教育/文化设施则明确提示先打开服务图层定位，不伪造不可建造动作。
- 新增阶段顾问单测，验证无医疗设施时的城市级提示、地图定位和建议工具；不改变服务系统对已存在设施的工人、库存、道路、容量和收入压力判定。
- 目标 GREEN：全量 46 个测试文件、293 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将医疗、教育、文化设施补入同年代建筑定义、建造菜单和服务规则，使缺失设施卡可以从诊断继续到真实营造。
2. 在允许本地 socket 的环境补跑居民治理、服务短板和缺失设施浏览器场景，取得截图/日志证据。
3. 继续推进教育、阶层和城市阶段差异的可见运行反馈。

## 2026-07-17 第一百三十三轮：医疗、教育与文化设施进入运行时

- 将草木药铺、溪山书院、水上戏台从静态建筑目录接入运行时建筑定义：拥有时代阶段、岗位、容量、占地、入口、升级上限和营造报价。
- 药铺现在对应 `health` 服务并生产药材；书院对应 `education` 服务；戏台对应 `entertainment` 服务。三者继续复用真实服务队列、居民需求压力、道路可达、有效工人和服务容量机制。
- 建造菜单按城市阶段解锁：药铺进入商贸镇开放，书院和戏台进入府镇开放；Prefab 映射同步接入现有动态建筑渲染资产。
- 升级经济审计由 40 个节点扩展为 64 个节点，新增设施的 0–8 级收益、维护和服务吞吐均进入同一审计口径。
- 目标 GREEN：全量 46 个测试文件、294 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 增加药铺/书院/戏台的运行时营造场景，验证从建造、配工、服务访问到居民需求恢复的可重复闭环。
2. 在允许本地 socket 的环境补跑服务设施建造与居民治理浏览器场景，取得截图/日志证据。
3. 继续将同年代生产、仓储、住宅和公共设施扩展为可审计的城市阶段组合，避免只增加孤立建筑。

## 2026-07-17 第一百三十四轮：服务设施建造到居民恢复闭环

- 新增 `service-facility-runtime` 可重复调试场景：扩充居民样本进入商贸镇、制造健康短板，并通过真实运行时重新计算阶段与人口状态。
- 新增药铺 QA 场景，实际调用公共预览/建造接口，再接入配工、药材库存、服务访问和健康恢复；验证结果经过模拟 tick 的事件时间线。
- 新增 `qa:service-facility-runtime` 入口，并将调试场景接入 URL 参数解析，后续可在浏览器中复用同一状态。
- 目标 GREEN：全量 47 个测试文件、295 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将药铺闭环扩展为书院、戏台，并覆盖缺工、断路、缺库存和需求恢复四类服务结果。
2. 在允许本地 socket 的环境补跑服务设施浏览器场景，取得真实点击、居民服务访问和建筑状态证据。
3. 继续建立同年代生产—仓储—住宅—公共设施组合，避免服务设施成为孤立可建物。

## 2026-07-17 第一百三十五轮：公共服务三类设施闭环与故障解释

- 将药铺、书院、戏台统一纳入可重复的运行时 QA 场景：真实调用预览、建造、配工、服务访问和需求恢复；三类设施分别验证 health、education、entertainment 需求提升。
- 新增缺工、缺少药材、断路三类药铺故障夹具，服务建筑状态与居民需求压力都保留同一真实失败原因，不再把“建成但不运行”隐藏成普通数值变化。
- `service-facility-runtime` 继续作为 QA-only 阶段夹具，不改变生产模拟的阶段推导；URL 入口与定向 QA 命令可复用同一确定性场景。
- 目标 GREEN：全量 47 个测试文件、298 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 把三类公共服务接入长期人口、就业、财政反馈，验证服务恢复不是一次性需求加分而是持续运行结果。
2. 在允许本地 socket 的环境补跑 `service-facility-runtime` 浏览器场景，取得真实建造、居民访问和故障提示截图/日志。
3. 继续补齐同年代住宅、生产、仓储、公共设施组合，并把阶段升级后的城市空间变化接入可见地图。

## 2026-07-17 第一百三十六轮：公共服务进入城市吸引力反馈

- `CityMetrics` 新增 `publicServiceCoverage`，由真实家庭的医疗、教育、娱乐需求均值计算，并在城市指标栏单独显示。
- 城市吸引力不再只读取总满意度：公共服务覆盖作为独立权重参与外来人口吸引，使服务恢复能影响后续迁入条件，而不是只改变单户数值。
- 新增引擎回归，验证同一住房、岗位、财政和物流条件下，公共服务需求短板会同时降低服务覆盖与城市吸引力；服务设施 QA 也验证服务恢复后的覆盖读数回升。
- 目标 GREEN：全量 47 个测试文件、299 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将公共服务覆盖继续接入迁入/迁出统计和财政维护反馈，形成可观察的长期人口—财政链路。
2. 在允许本地 socket 的环境补跑服务设施浏览器场景，验证指标栏、居民服务访问和故障提示的真实画面。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百六十轮：分析批次传输与失败重试协议

- 新增 `CityNoticeAnalyticsTransport` 与 `CityNoticeAnalyticsDispatcher`，把 durable outbox 与真实分析服务隔离。
- 只有传输器明确返回的事件 ID才会从本地队列删除；未配置传输器、网络异常和部分确认都会保留未确认事件。
- `GameRuntime` 增加可选传输器和 `flushCityNoticeAnalytics()`；默认仍保持纯离线，不伪造上报成功。
- 本轮定向 46 项通过；全量 47 个测试文件、323 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 接入可替换的 HTTP/批量分析服务适配器，补充请求超时、响应校验和服务端幂等键。
2. 在允许本地 socket 的环境补跑浏览器重载恢复、分析批次确认和真实面板交互证据。
3. 继续推进商业级美术、建模、动画和完整文明内容生产，当前代码验证不等于商业级最终品质。

## 2026-07-17 第一百六十一轮：HTTP 分析服务适配器

- 新增 `HttpCityNoticeAnalyticsTransport`，支持批次 POST、确定性幂等键、请求超时中止、非 2xx 拒绝和响应结构校验。
- 服务端只确认本批次中的合法事件 ID；未知 ID被过滤，未确认事件继续留在本地 durable outbox。
- 本轮定向 35 项通过；全量 47 个测试文件、326 项测试通过；生产构建 2357 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 在允许本地 socket 的环境补跑分析 outbox 浏览器证据，验证刷新恢复、批量发送和确认后的队列变化。
2. 为生产 endpoint 补充服务端幂等存储契约与鉴权配置边界；当前仅完成前端适配器。
3. 推进商业级美术、建模、动画和完整文明内容生产线，补做实际可见的阶段资产审计。

## 2026-07-17 第一百五十五轮：财政压力可定位治理提示

- `cityNotices` 新增 `finance` 提示类型：当最近财政周期的阻塞、物流积压、库存压力建筑或居民压力任一项上升时，生成“运营压力在上升”提示。
- 提示复用真实财政差值，并按当前阻塞建筑 → 物流热点 → 居民关联建筑的顺序解析定位目标；治理入口可直接跳到对应运行对象。
- 新增回归覆盖“财政压力上升→真实阻塞建筑定位→warning 提示”的完整投影链。
- 目标 GREEN：全量 47 个测试文件、313 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。
- 浏览器验收仍受当前环境禁止本地 socket 监听限制，未宣称真实点击通过。

## 4. 下一轮默认任务（更新）

1. 在允许本地 socket 的环境补跑财政结算、压力提示、建筑定位和多段阻塞时间线浏览器场景，并记录真实点击证据。
2. 扩展财政压力提示的确认、消退和重复抑制证据，避免同一周期反复打扰玩家。
3. 继续推进商业级美术、建模、动画和同年代城市内容生产，当前代码模拟闭环不等于商业上线完成。

## 2026-07-17 第一百五十六轮：财政提示生命周期去打扰

- `CityNoticeTracker` 已验证财政压力提示在同一压力周期内只产生一次新提示，不会随模拟刻推进重复弹出。
- 当下一财政周期压力归零后提示自动解除；后续压力再次上升时重新触发，形成“上升提示→维持静默→消退→再次异常再提示”的生命周期。
- 新增两项回归测试，覆盖重复抑制和恢复后重新武装；全量 47 个测试文件、315 项测试通过，生产构建 2356 个模块通过。
- 浏览器真实点击证据仍受本地 socket 限制，当前仅确认了运行时和单元回归行为。

## 4. 下一轮默认任务（更新）

1. 在 socket 可用环境补跑财政提示的真实点击、定位、消退和重复抑制浏览器场景。
2. 将同一生命周期策略扩展到其他高优先级城市提示，避免粮食、物流和人口提示在长时间运行中反复打扰。
3. 继续推进商业级美术、建模、动画与同年代城市内容生产。

## 2026-07-17 第一百五十七轮：高优先级提示生命周期审计

- 为粮食短缺、物流阻塞和迁移等待补充与财政提示一致的生命周期回归：同一问题持续时不重复触发，状态恢复后清除，再次发生时重新触发。
- 这验证了 `CityNoticeTracker` 对主要高优先级城市反馈的通用边沿语义，而不是只为财政提示单独编写特例。
- 目标 GREEN：全量 47 个测试文件、318 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。
- 浏览器真实点击证据仍缺失，原因是当前环境禁止本地 socket 监听。

## 4. 下一轮默认任务（更新）

1. 在 socket 可用环境补跑城市提示流的真实点击、地图定位、恢复消退和重复抑制场景。
2. 将提示生命周期状态接入分析事件，记录首次出现、确认、恢复和再次发生，供商业化运营审计使用。
3. 继续推进商业级美术、建模、动画和同年代城市内容生产；代码闭环仍不等于最终商业品质。

## 2026-07-17 第一百五十八轮：城市提示分析生命周期事件

- 新增 `CityNoticeLifecycleEvent` 协议，统一记录 `activated`、`retriggered`、`resolved`、`acknowledged` 四种提示生命周期阶段。
- `CityNoticeTracker` 负责从真实城市状态产生事实，`GameRuntime` 暴露消费接口，界面“看一眼/知道了”动作会写入确认事件；分析层没有侵入模拟系统。
- 回归覆盖首次激活、同周期去重、用户确认、状态恢复和再次异常；全量 47 个测试文件、319 项测试通过，生产构建 2356 个模块通过。
- 浏览器真实点击证据仍受当前环境禁止本地 socket 监听限制。

## 4. 下一轮默认任务（更新）

1. 为提示生命周期事件增加持久化/批量上报边界，确保离线期间不丢失且不重复上报。
2. 在 socket 可用环境补跑提示流真实点击、定位、确认和恢复场景。
3. 继续推进商业级美术、建模、动画和同年代城市内容生产。

## 2026-07-17 第一百五十九轮：提示分析离线持久化与批次去重

- 新增 `CityNoticeAnalyticsQueue` durable outbox：生命周期事件写入浏览器 `localStorage`，重载后可恢复，存储损坏或隐私模式失败不会阻塞游戏启动。
- 事件 ID 改为 `noticeId + phase + tick` 的稳定键，避免运行时重启后同一事实再次上报；队列提供 `peek`、`consume`、`acknowledge` 和数量查询，支持接入真实网络批量发送。
- `GameRuntime` 在构造、刷新、重建和模拟推进时捕获通知事实；界面确认继续产生 acknowledged 事件，模拟引擎不感知分析存储。
- 目标 GREEN：全量 47 个测试文件、320 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。
- 真实分析服务、网络失败重试策略和浏览器点击证据仍未完成。

## 4. 下一轮默认任务（更新）

1. 为分析批次定义网络发送适配器边界和失败重试/幂等协议，不在本地队列中假装已上报。
2. 在 socket 可用环境补跑重载恢复、确认、定位和批量消费浏览器场景。
3. 继续推进商业级美术、建模、动画和同年代城市内容生产。

## 2026-07-17 第一百五十一轮：阻塞持续期间增量审计

- `BuildingBlockageConsequenceDelta` 已加入公共契约；建筑阻塞开始时保存库存、关联物流、缺勤和居民压力基线，恢复时计算期间增量。
- `SimulationEngine` 已回写 `blockedSinceTick`，并在真实 `step()` 生命周期中生成起始快照、恢复快照与增量；建筑详情和城市时间线展示库存、物流和压力变化方向。
- 新增真实引擎回归，验证阻塞持续 2 刻、库存从 2 增至 5 时生成 `inventoryDelta: 3`，修复了起始刻未回写导致持续时长为 0 的缺口。
- 目标 GREEN：全量 47 个测试文件、309 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将阻塞期间增量接入财政结算与居民治理周期，区分“库存缓解但财政恶化”等真实城市状态。
2. 修正阻塞原因在持续期间发生变化时的生命周期分段，避免同一建筑出现无法闭合的审计链。
3. 在允许本地 socket 的环境补跑真实浏览器时间线/建筑详情场景；商业级美术、建模、动画和完整文明内容继续按生产流水线推进。

## 2026-07-17 第一百五十三轮：阻塞原因变化生命周期分段

- `SimulationEngine` 现在会在阻塞原因从缺料、仓满、断路/物流等一种原因变化为另一种原因时，先闭合旧段，再以当前模拟刻开启新段。
- 每个原因段独立保存起始后果、恢复后果、持续时长和增量；建筑详情与城市时间线不再把多种原因错误合并成一段。
- 新增真实引擎回归覆盖“缺料 → 断路 → 恢复”两段生命周期；全量 47 个测试文件、310 项测试通过，生产构建 2356 个模块通过，`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 为财政运营压力增加前后结算周期对比，并把异常压力写入可定位城市时间线记录。
2. 在允许本地 socket 的环境补跑多段阻塞时间线与建筑详情浏览器场景。
3. 商业级美术、建模、动画和完整文明内容继续按生产流水线推进，当前工程验证不等于商业级上线完成。

## 2026-07-17 第一百五十四轮：财政压力周期对比与时间线事件

- 财政系统现在在每次结算刻发出 `fiscal-settlement` 事件，并在 `FiscalSettlementRecord` 保存当前运营压力与相对上一结算周期的差值。
- 城市时间线新增财政结算记录，结构化展示库银、税收、维护、阻塞建筑、物流积压和居民压力变化；居民治理卡同步显示最近结算周期的压力变化。
- 新增财政系统与时间线回归，验证跨周期阻塞/库存压力差值会被真实计算并投影；全量 47 个测试文件、312 项测试通过，生产构建 2356 个模块通过，`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将财政压力上升识别为可定位的城市治理提示，避免只展示数字而没有行动入口。
2. 在允许本地 socket 的环境补跑财政结算与多段阻塞时间线浏览器场景。
3. 继续商业级美术、建模、动画与文明内容生产，并保持每轮产物可回归验证。

## 2026-07-17 第一百五十二轮：阻塞增量接入财政周期

- `FiscalSettlementRecord` 新增同刻运营压力快照：阻塞建筑、物流积压、库存压力建筑和居民压力户数，随真实财政结算持久化。
- 居民治理面板新增最近财政结算的运营压力读数，能对照当前阻塞状态判断压力是否在财政周期中被带入城市成本与治理问题。
- 财政、居民治理、模拟引擎和长时间文明回归保持兼容；全量 47 个测试文件、309 项测试通过，生产构建 2356 个模块通过，`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 修正阻塞原因变化时的生命周期分段，让缺料→断路→恢复等状态各自闭合并保留增量。
2. 为财政压力快照补充前后周期对比，并在城市时间线形成可定位的运营—财政记录。
3. 在允许本地 socket 的环境补跑真实浏览器时间线/建筑详情场景；商业级美术、建模、动画和完整文明内容继续按生产流水线推进。

## 2026-07-17 第一百五十轮：阻塞同刻后果因果链

- `BuildingBlockageConsequences` 进入模拟事件契约，记录阻塞当刻缺勤工人、关联物流订单、库存占用/容量和受影响居民压力。
- 引擎在阻塞开始与恢复的真实状态边沿采集这些后果；时间线和建筑详情同步展示，避免只给出抽象的“运行异常”。
- 时间线回归覆盖缺料阻塞的库存、物流和居民压力数据；全量回归保持通过。
- 目标 GREEN：全量 47 个测试文件、308 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将同刻后果扩展为阻塞持续期间的增量变化，记录物流积压、库存变化和居民压力的前后差值。
2. 在允许本地 socket 的环境补跑时间线和建筑详情浏览器场景，验证真实交互证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十九轮：阻塞生命周期时间线审计

- 模拟引擎在真实建筑状态从正常进入运行阻塞、或从阻塞恢复时生成生命周期事件；服务恢复沿用专用恢复事件，避免重复记录。
- 城市时间线新增运行类记录，明确阻塞原因、开始刻、持续刻数和恢复后的建筑状态；建筑详情新增“运行状态审计”卡片，可回溯该建筑的开始与恢复记录。
- 新增时间线回归，验证缺料阻塞从第 30 刻开始并在第 35 刻恢复时形成可检查的开始/恢复数据。
- 目标 GREEN：全量 47 个测试文件、308 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将阻塞生命周期与居民缺勤、物流订单和库存变动进行同刻关联，形成可解释的因果链。
2. 在允许本地 socket 的环境补跑时间线和建筑详情浏览器场景，验证真实交互证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十八轮：运行阻塞持续时间与治理后果

- `BuildingEntity.blockedSinceTick` 由生产、服务和物流系统在阻塞原因首次出现或变化时写入，恢复营业、成功生产和升级切换时清理，升级状态不会被误判为运行阻塞。
- 居民治理摘要新增阻塞建筑数、最长阻塞建筑与持续刻数、物流待处理单数和库存压力建筑数；面板用一行后果审计把居民、物流和库存影响放在同一处。
- 新增治理回归，验证仓满阻塞从第 25 刻持续到第 32 刻时显示 7 刻，并正确归因仓满与库存压力。
- 目标 GREEN：全量 47 个测试文件、307 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将阻塞持续时间接入城市时间线和建筑详情，形成“开始阻塞—造成后果—恢复”的闭环审计。
2. 在允许本地 socket 的环境补跑人口治理与阻塞反馈浏览器场景，验证真实面板和画布渲染。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十七轮：阻塞原因动态反馈

- 建筑阻塞状态继续沿用真实 `statusReason`，缺工、缺料、断路/物流失败、仓满分别保留人员、容器、运输和库存图形提示。
- 阻塞反馈加入随模拟时钟变化的呼吸强度，和恢复脉冲共用建筑状态图层；没有额外并行状态或脱离模拟的动画计时器。
- 渲染定向 13 项通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 在允许本地 socket 的环境补跑服务恢复/阻塞浏览器场景，取得画布和详情面板的真实渲染证据。
2. 将阻塞持续时间与居民缺勤、物流队列、库存压力联动到同一治理摘要。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十六轮：服务恢复动态渲染

- `BuildingVisual` 读取同一份 `cityTimeline` 恢复记录，在服务瓶颈解除后的 12 个模拟刻内播放短时恢复脉冲；脉冲会随建筑状态和模拟时钟更新，不是静态贴图。
- 动效仅消费真实 `service-bottleneck-cleared` 事实，不改变服务系统或额外制造 UI 状态；建筑详情、治理面板、时间线和画布共用同一事件源。
- 渲染定向 13 项通过；全量 47 个测试文件、306 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将缺工、缺料、断路等阻塞原因接入同一套动态状态动效，并区分恢复反馈颜色和图标。
2. 在允许本地 socket 的环境补跑服务恢复浏览器场景，取得治理卡、详情卡、时间线和画布动效的真实渲染证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十五轮：恢复审计结构化数据

- 时间线新增 `CityTimelineServiceRecoveryAudit` 与 `CityTimelineServiceDeliveryAudit`：服务恢复记录保留需求类型、建筑状态 `before→after`、清除压力户数、最长压力时长；服务完成记录保留最近一户居民需求 `before→after`。
- 建筑详情将结构化恢复事实渲染为状态、压力、财政影响和需求变化四项审计读数，仍由 `cityTimeline` 真实快照驱动，不创建并行 UI 状态。
- 定向回归 78 项通过；全量 47 个测试文件、305 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将结构化服务恢复审计接入动态渲染层，显示建筑阻塞/恢复状态与短时恢复动效。
2. 在允许本地 socket 的环境补跑服务恢复浏览器场景，取得治理卡、详情卡和时间线的真实渲染证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十四轮：恢复事件界面消费

- 瓶颈治理面板显示最近一次服务恢复记录，并可直接定位恢复建筑。
- 建筑详情抽屉新增“最近服务恢复”卡，展示恢复刻度、压力清除结果和真实运行时来源。
- 新增 UI 文案回归，保证服务恢复事件在治理面板与建筑详情使用同一条时间线事实。
- 目标 GREEN：全量 47 个测试文件、305 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 为服务恢复卡补充恢复前后需求数值、财政维护成本和建筑状态变化的结构化展示。
2. 在允许本地 socket 的环境补跑服务恢复浏览器场景，取得面板真实渲染证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十三轮：服务瓶颈解除与建筑恢复审计

- 服务系统新增 `service-bottleneck-cleared` 运行时事件：设施从缺工、缺资源、断路或收入不足等阻塞原因恢复后，记录恢复的服务类型、解除原因、清除压力户数和最长压力时长。
- 建筑状态从 `blocked` 恢复为 `serving` 的变化由真实服务派发驱动，并进入城市时间线；不是 UI 层推断。
- 全量 47 个测试文件、304 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将服务瓶颈解除事件接入治理面板和建筑详情，提供可定位的恢复证据。
2. 在允许本地 socket 的环境补跑服务恢复浏览器场景，取得治理卡与时间线真实渲染证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十二轮：服务恢复前后审计

- `service-delivered` 事件新增可选需求前后值，居民时间线可显示服务恢复幅度，而不是只有“服务完成”结果。
- 服务完成事件复用最近真实财政结算快照，展示公共服务维护支出与库银前后变化，形成“服务恢复—财政周期”的可追溯链路。
- 保持旧事件兼容：没有需求前后值或财政历史的旧存档仍按原有文案渲染。
- 目标 GREEN：全量 47 个测试文件、303 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将服务恢复审计扩展到服务瓶颈解除、建筑状态恢复和居民需求压力清除。
2. 在允许本地 socket 的环境补跑服务恢复浏览器场景，取得治理卡与时间线真实渲染证据。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十轮：人口事件与财政周期关联

- `EconomyState` 记录最近财政结算刻度；城市时间线的人口迁出记录携带当时库银、总维护和公共服务维护支出。
- 迁出记录同时保留服务瓶颈与具体服务建筑，形成“需求短板 → 服务故障 → 家庭离城 → 财政压力”的单条可追溯链路。
- 新增时间线回归，验证财政快照与公共服务维护成本会随人口事件持久化展示；旧事件没有财政上下文时仍兼容。
- 目标 GREEN：全量 47 个测试文件、302 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将财政快照和人口事件链扩展到税收、维护结算与服务恢复事件，支持同一周期前后对比。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，验证财政关联文本真实渲染。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百四十一轮：财政结算历史与周期对比

- `EconomyState` 持久化最近 24 次财政结算，记录结算前库银、税收、总维护和公共服务维护成本，支持跨周期追踪财政压力。
- 人口时间线只在真实财政结算存在时注入财政快照，迁出记录显示同一结算周期的库银 `before→after` 与维护拆分，避免历史事件被伪造的零值财政数据污染。
- 运行时桥接改用结算历史作为唯一来源，保证人口事件、服务短板和财政周期引用同一份结算事实。
- 目标 GREEN：全量 47 个测试文件、302 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 增加服务恢复事件的前后状态审计，并与对应财政结算周期关联。
2. 在允许本地 socket 的环境补跑人口时间线与财政周期浏览器场景，验证真实面板渲染。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百三十九轮：离城居民生命周期档案

- `SimulationAdvanceResult` 增加离城居民临时档案通道；引擎在删除家庭实体前保存成员数、劳动力、就业状态、职业、原住房和满意度。
- 城市时间线消费该档案，离城记录不再显示空的“0 人/0 劳动力”占位信息；既有 `household-migrated` 事件结构保持兼容。
- 增加时间线和引擎回归，验证离城档案在真实迁出 tick 生成，并能被城市时间线正确渲染。
- 目标 GREEN：全量 47 个测试文件、302 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将离城档案与城市时间线的迁出原因、服务瓶颈和财政周期关联，形成单条可追溯人口事件链。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，验证离城居民卡片真实渲染。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百三十八轮：人口流动结构审计

- `PopulationFlowLedger` 扩展为可解释的流动结构账本：迁入按住房类型累计，迁出按迁出原因、住房类型、职业类别以及就业/待业劳动力累计。
- 居民治理卡新增人口流动审计摘要，直接从模拟存档派生主要迁出原因和离城职业，避免只显示一个无法解释的净值。
- 新增迁出结构回归，验证关键需求短板、住房类型和待业劳动力会在家庭离城的同一 tick 被持久化记录。
- 目标 GREEN：全量 47 个测试文件、301 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将人口流动结构接入城市时间线的离城居民档案，展示成员数、住房、职业和满意度，而不是只显示“一个家庭”。
2. 在允许本地 socket 的环境补跑居民治理浏览器场景，验证结构审计面板真实渲染。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。

## 2026-07-17 第一百三十七轮：人口流动账本与服务财政反馈

- 新增 `PopulationFlowLedger`，迁入和迁出在居民真正落户/离城时累计户数、人口数和最近发生刻，旧存档缺失时自动补齐为零。
- `CityMetrics` 增加迁入、迁出、净迁入读数；居民治理卡显示净迁入，并显示公共服务覆盖与最近公共服务维护支出，形成“服务—吸引—迁入—财政”的持久反馈链路。
- 财政系统在结算时单独核算服务类建筑维护成本，同时保留全量维护成本，便于后续做公共服务扩张与财政压力决策。
- 目标 GREEN：全量 47 个测试文件、300 项测试通过；生产构建 2356 个模块通过；`git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将迁入/迁出账本扩展到按迁出原因、成员职业和住房类型分解，接入城市时间线审计。
2. 在允许本地 socket 的环境补跑服务设施浏览器场景，验证居民治理卡的新指标真实可见。
3. 继续补齐同年代公共设施的等级差异、服务半径和城市空间可见变化。
## 2026-07-17 第一百六十二轮：美术资产覆盖审计落地

- 新增首批六类金样的商业交付覆盖审计：当前 `0/6` 达到生产绿灯，`2/6` 仅有契约 manifest，`1/6` 为等级覆盖红灯，`3/6` 尚未建立样本目录。
- 明确的首个真实美术门禁是 `ART-GOLD-SLICE-REBUILD-01`：补齐三类缺失金样、补齐码头五个缺失等级，并提供 Blender/DCC 源文件与运行时图集/二进制导出。
- 美术、建模、动画仍是商业级交付的主要未完成项；本轮产出是可重复的红灯报告，不代表最终画面已完成。

## 4. 下一轮默认任务（更新）

1. 由美术生产线补齐 `windfield-rice`、`main-granary`、`main-kiln` 的金样 manifest、DCC 源文件和运行时导出，并逐项过覆盖门禁。
2. 重建 `main-pier` 的 L2/L3/L5/L6/L7，不允许用复制等级或亭/船替代建筑主体差异。
3. 在允许本地 socket 的环境补跑分析 outbox 浏览器证据；工程侧继续保持服务端幂等和鉴权待接入状态。
## 2026-07-17 第一百六十三轮：建筑视觉成长契约接入

- 建筑美术验收从“层级字段存在”升级为“可读的建筑差异与繁荣成长”：每类建筑必须有自己的功能轮廓与材质语言，L0–L8 必须逐级提供环境、活动和主体变化证据。
- 覆盖门禁当前结果仍为 `0/6` 生产绿灯、`3/6` 红灯、`3/6` 缺失样本；工程契约已就绪，但真实模型、图集、动画和源文件尚未交付。
- 这轮只完成生产约束和验证，不宣称画面或建筑资产已经达到商业级。

## 4. 下一轮默认任务（更新）

1. 为 `main-homes`、`main-eatery`、`main-pier` 补写真实视觉身份与 L0–L8 成长数据，并由美术产物反向校验，不得填写占位描述。
2. 建立 windfield-rice、main-granary、main-kiln 的完整金样目录，至少交付模型源、9 级主体、运行时导出和动画 manifest。
3. 在真实产物出现后增加跨建筑唯一性检查，禁止不同建筑复用同一轮廓族和功能识别。

## 2026-07-17 第一百六十八轮：DCC 建模—动画—导出流水线契约

- 新增 Blender 4.x LTS 执行脚本 `tools/art-pipeline/blender/build-gold-slice.py`：强制九级集合、语义命名、锚点、碰撞/遮挡集合、可动部件 action、L0/L8 和关键里程碑差异，并输出九级透明预览与 DCC provenance manifest。
- 新增 `npm run asset:pipeline:contract`，对脚本和建模规格执行漂移门禁；当前通过 10 项脚本门禁和 5 项规格门禁。
- 新增美术机执行手册，明确源 `.blend`、九级预览、分层图集、动画 manifest、碰撞代理、LOD 预算和 Pixi 浏览器验收的交付物边界。
- 新增 `resolvePrefabAnimationPlan`，将 Prefab manifest 的状态槽位、播放速率来源、LOD 策略和生产/施工进度转换为确定性的运行时播放计划，并覆盖施工、营业和 LOD 过滤测试。
- 这轮没有伪造 Blender 模型或动画产物：当前机器仍未安装 Blender，商业美术覆盖审计继续保持红灯。

## 4. 下一轮默认任务（更新）

1. 在美术机执行首个金标 `main-pier` 的真实 Blender 源资产与九级导出，并将 DCC provenance 接入覆盖审计。
2. 将 `resolvePrefabAnimationPlan` 接入实际 AnimatedSprite、部件变换和粒子驱动，优先覆盖施工、营业、生产、仓满和缺料状态。
3. 在允许本地 socket 的环境补跑浏览器像素、透明边缘、动效和帧率证据。

## 2026-07-17 第一百六十九轮：Prefab 动画计划接入动态渲染

- `BuildingVisual` 现在在每次同步时解析当前建筑的 Prefab 状态槽位，并把场景时间、生产进度、施工进度和 LOD 策略转换为动态层可消费的播放状态。
- 动态层标签与运动相位会携带真实槽位，例如 `storage-full`、`production-primary`，施工、仓满和生产不再只依赖统一程序化状态判断。
- 回收对象时清理动画计划，避免对象池复用把旧建筑的动画状态带入新实体。
- 全量 51 个测试文件、333 项测试通过；生产构建 2360 个模块通过；DCC 契约、运行时 PNG 审计和 `git diff --check` 通过。

## 4. 下一轮默认任务（更新）

1. 将播放计划进一步接入真实 AnimatedSprite、部件变换和粒子图集；当前仍由程序化 motion layer 承担无真实图集时的降级表现。
2. 在 Blender 美术机完成 `main-pier` 源资产与九级透明预览，之后让覆盖审计识别 DCC provenance。
3. 在可监听环境补跑浏览器真实像素、透明边缘、状态切换与帧率证据。

## 2026-07-17 第一百七十轮：真实图集播放驱动骨架

- 新增 `BuildingAnimationProvider`，支持以建筑、等级、槽位和 clip 查询动画帧，并提供 Pixi `Spritesheet` 适配器；缺失图集或 clip 时返回明确的未命中结果。
- 新增 `BuildingAnimationDriver`：按 `PrefabAnimationPlan` 创建并复用 `AnimatedSprite`，使用确定性进度选帧，接入槽位标签、尺寸、锚点、循环和对象池 reset。
- `DynamicScene` 增加可选动画 Provider 注入点；没有真实图集时仍保持现有身份化/程序化降级，不把空 Provider 误报为商业资产。
- 全量 52 个测试文件、336 项测试通过；生产构建 2361 个模块通过；DCC 契约、运行时 PNG 审计和 `git diff --check` 通过。
- 商业美术覆盖仍未转绿：当前仓库仍缺真实 Blender 源文件、分层 spritesheet PNG/JSON、部件动画和浏览器帧率证据。

## 4. 下一轮默认任务（更新）

1. 在美术机完成 `main-pier` 的真实 `.blend`、九级透明预览和首套状态图集，并用 Provider 注入浏览器场景。
2. 将同一图集链扩展到部件变换与粒子实例，覆盖施工、营业、生产、仓满和缺料状态。
3. 在可监听环境补跑浏览器像素、透明边缘、状态切换与帧率证据。

## 2026-07-17 第一百七十一轮：图集清单异步加载与场景注入

- 新增 `BuildingAnimationAtlasManifest` 与 `loadBuildingAnimationAtlases`，把 DCC 导出的建筑/等级 atlas URL 变成可校验、可并行加载的运行时索引。
- 加载前拒绝缺字段和重复建筑等级键，避免错误资产在网络完成顺序下静默覆盖；加载后直接复用现有 `Spritesheet` Provider 和 `AnimatedSprite` 驱动。
- `SimulationCanvas` 增加可选 atlas manifest 注入，异步加载成功才启用图集层；加载失败自动保留程序化回退，不影响城市模拟继续运行。
- 全量 53 个测试文件、338 项测试通过；生产构建 2361 个模块通过；DCC 契约、运行时 PNG 审计和 `git diff --check` 通过。
- 当前仍没有真实 atlas manifest、spritesheet PNG/JSON 或 Blender 导出物，因此本轮完成的是生产接线，不是美术资产完成。

## 4. 下一轮默认任务（更新）

1. 在美术机完成 `main-pier` 真实 `.blend`、九级透明预览和状态图集，提交经过覆盖审计的 manifest。
2. 将真实图集注入 `SimulationCanvas`，补施工/营业/生产/仓满/缺料的浏览器状态切换证据。
3. 将导出的部件和粒子槽位接入独立层，测量目标设备上的帧率与显存预算。

## 2026-07-17 第一百七十二轮：部件变换与粒子槽位消费

- `BuildingAnimationDriver` 现在消费 `part-transform` 槽位：通过部件 Provider 获取 DCC 纹理，按命名锚点定位，并按确定性播放进度驱动轮轴旋转或部件摆动。
- `particle` 槽位现在消费独立粒子 Provider，生成可复用的粒子精灵，按同一动画计划的 phase、锚点、透明度和高度漂移更新；不存在粒子纹理时不会生成伪粒子。
- `DynamicScene` 与 `SimulationCanvas` 暴露 `BuildingAnimationDriverOptions`，美术资产到位后可同时注入部件、锚点和粒子资源，不需要改动城市模拟逻辑。
- 全量 53 个测试文件、339 项测试通过；生产构建 2361 个模块通过；DCC 契约、运行时 PNG 审计和 `git diff --check` 通过。
- 真实 Blender 部件/粒子导出与浏览器帧率证据仍未交付，商业美术覆盖保持红灯。

## 4. 下一轮默认任务（更新）

1. 在美术机交付 `main-pier` 的真实部件纹理、锚点表和粒子纹理，并映射到本轮 Provider 接口。
2. 用真实状态图集和部件/粒子资源完成 `main-pier` 五状态浏览器验收。
3. 建立粒子数量、纹理尺寸、对象池命中率和帧率的自动基准，作为后续建筑量产门禁。

## 2026-07-17 第一百七十三轮：动画资源与运行时预算门禁

- 金样资产校验新增动画运行时预算：总槽位不超过 20，部件变换槽位不超过 4，粒子槽位不超过 2，单槽位部件不超过 4、锚点不超过 8。
- 粒子槽位强制 LOD3 关闭，LOD2 不得使用 full，避免远景继续创建高成本粒子；这些约束在导出前失败，而不是等浏览器卡顿后才发现。
- 自测新增超预算清单用例；`main-pier`、gold samples 和现有生产契约均通过。
- 全量 53 个测试文件、339 项测试通过；生产构建 2361 个模块通过；运行时 PNG 审计通过。
- 这仍是预算门禁，不等于真实设备性能证明；Blender 资产、浏览器帧率、显存和对象池实测仍待真实资源接入。

## 4. 下一轮默认任务（更新）

1. 在美术机交付 `main-pier` 真实图集、部件纹理、锚点表和粒子纹理，执行完整覆盖审计。
2. 建立真实资源驱动的动画压力基准，覆盖 300 栋建筑和 150 个可见实体目标。
3. 在可监听浏览器环境收集状态切换、粒子数量、纹理内存和帧率证据。

## 2026-07-17 第一百七十四轮：动画运行时压力基准

- 新增 `BuildingAnimationDriver.getDiagnostics()`，记录池化序列/部件/粒子数量、可见子节点和活动槽位。
- 新增 300 建筑宿主 / 150 可见建筑压力场景：每个可见建筑包含 1 个图集序列、1 个部件变换和 3 个粒子节点，二次更新保持同一批 Sprite 引用。
- 新增 `npm run qa:animation-runtime-budget`，将运行时节点上限和池化行为纳入回归门禁。
- 全量 54 个测试文件、340 项测试通过；生产构建 2361 个模块通过；资产自测、gold samples、DCC 契约、运行时 PNG 审计和 `git diff --check` 通过。
- 证据边界：该基准验证 Pixi 对象生命周期和节点上限，不代表真实浏览器 GPU 帧率、显存或 Blender 资产已经交付。

## 5. 下一轮默认任务（更新）

1. 在美术机交付 `main-pier` 真实图集、部件纹理、锚点表和粒子纹理，执行覆盖审计。
2. 将真实资源接入压力基准，采集浏览器帧率、纹理内存和状态切换证据。
3. 将同一门禁扩展到所有已完成 DCC 资产，并继续保留缺失真实资产时的红灯。

## 2026-07-17 第一百七十五轮：商业资源包验收门禁

- 新增 `production-package-audit`，把商业美术交付拆成四类硬证据：九级透明预览、DCC provenance、运行时 atlas/JSON/锚点，以及七类运行状态证据。
- 新增完整包/缺证据包自测；完整包为 GREEN，删除 `state-evidence.json` 后立即为 RED，证明门禁不会只看模板 JSON。
- 对六类金样执行真实目录审计，当前全部 RED：仓库尚无可验收的 `.blend` 导出包、运行时图集、锚点清单和状态证据。这一结果符合当前真实状态，不能用现有 252 张静态 PNG 替代。
- 新增 `npm run asset:production-package:audit` 与 `npm run asset:production-package:self-test`，并将交付要求写入 DCC 执行手册。

## 6. 下一轮默认任务（更新）

1. 美术机按资源包契约交付 `main-pier` 的九级源资产、atlas、锚点和七状态证据。
2. 通过生产包门禁后接入 `BuildingAnimationAtlasLoader` 和部件/粒子 Provider。
3. 在真实浏览器中完成五类运行状态、透明边缘、锚点和性能采样。

## 2026-07-17 第一百七十六轮：居民生命周期状态审计

- 审计确认外来人口链路由同一 `SimulationEngine` 驱动：候选人以 `waiting` 状态出现在入城点，具备成员数、劳动力数、目标住房和耐心；有住房且城市吸引力达标后进入 `walking`，抵达后才创建家庭和居民工人实体。
- 修复迁出时序缺陷：职业与就业状态现在在删除工人实体前快照，离城档案、职业分类和就业居民流出统计不再错误降级为“待业”。
- 新增就业家庭迁出回归：验证候选/稳定居民的状态差异、就业居民离城后的职业档案、岗位释放和人口流动账本一致。
- 本轮只验证模拟状态和结构化证据，未宣称人物渲染已达到商业级；真实浏览器人物动画与外来人口入住前后视觉差异仍需资源包和可监听浏览器验收。

## 7. 下一轮默认任务（更新）

1. 把居民生命周期档案接入场景内人物外观状态：候选人、步行入住、稳定居民和离城前分别使用明确的动作/服饰/路径标签。
2. 在真实浏览器场景补采入住前后人物数量、位置、活动和帧率证据。
3. 继续保持商业美术资源包门禁为硬门槛，未通过 `.blend`、atlas、锚点和七状态证据前不标记商业美术完成。

## 2026-07-17 第一百七十七轮：居民生命周期场景接线

- `HouseholdState` 新增 `origin` 与 `settledTick`；外来家庭只有在真实迁入结算后写入 `origin: migrated`，没有创建独立的假人物。
- 动态场景现在区分候选人 walking/arriving/waiting 三种状态，并为刚完成入住的迁入居民显示 24 tick 的入驻高亮；颜色、状态标签和场景节点均来自 `SimulationSnapshot`。
- 定向回归 34 项通过；全量回归通过（54 个测试文件、342 项测试），生产构建 2361 个模块通过，动画运行时预算通过。
- 证据边界：当前验证的是数据到渲染层的结构化接线；真实浏览器像素、GPU 帧率、商业 DCC/atlas/粒子资源仍未交付，不能标记为商业级美术完成。

## 8. 下一轮默认任务（更新）

1. 在可监听真实浏览器中采集候选人到入住居民的画面、位置、数量和帧率证据。
2. 在美术机交付 `main-pier` 真实九级 DCC/atlas/锚点/状态包，接入当前动画 Provider。
3. 将生命周期状态扩展到居民活动路线、服饰变体和服务设施状态，并保持 300 栋/150 可见实体预算门禁。
## 2026-07-17 第一百七十八轮：居民生命周期浏览器证据与资源加载修正

- 修复居民生命周期调试场景：此前修改的是 `SimulationEngine.snapshot` 的结构化副本，未重新注入 live engine，导致浏览器无法看到候选家庭与入住家庭；现在通过重新创建引擎保持场景数据真实可见。
- 新增 `GameRuntime` 回归，验证候选家庭与外来家庭状态同时存在于运行时城市时间线。
- 浏览器 runner 改为仅在瓶颈抽屉未打开时点击，避免场景契约与 UI 初始状态重复切换。
- `civilization-resident-timeline` 真实浏览器场景通过：页面可见“居民状态：”“候选家庭”“外来家庭”。浏览器仍报告 WebGL `texImage2D: bad image data` 与 GPU stall warning；这不是功能失败，但仍是商业级渲染质量风险。
- 运行时建筑 PNG Provider 改为浏览器端使用 Image resource，绕过 Pixi 字符串资源的全局 Cache miss 警告；真实 atlas/DCC 资源到位后仍需继续定位图像数据警告。
- 全量测试、生产构建、动画预算、DCC 流水线契约、运行时 PNG 审计和生产包自测通过；真实商业生产包审计仍保持 RED。

## 9. 下一轮默认任务（更新）

1. 在真实美术机交付 `main-pier` 完整 DCC/atlas/锚点/状态包并通过生产包审计。
2. 继续补居民生命周期的浏览器像素、位置、数量、活动和目标设备帧率证据。
3. 对浏览器 `texImage2D` 警告做资源解码与 GPU 兼容性定位，不以警告过滤代替修复。
## 2026-07-17 第一百七十九轮：建筑资源预加载与首帧 WebGL 修正

- 建筑运行时 Provider 新增 `loadDefaultBuildingArtworkProvider`，按当前城市快照实际使用的建筑资产预加载九级 PNG，再创建场景，避免未解码 HTMLImageElement 被 Pixi 首帧上传。
- `SimulationCanvas` 在 DynamicScene 初始化前收集当前建筑对应的 Prefab asset id 并等待 Pixi Assets 完成加载；没有资源或资源失败时仍保留身份化程序化降级边界。
- 真实浏览器居民生命周期场景继续通过；上一轮的 `WebGL: INVALID_VALUE: texImage2D: bad image data` 已消失，只剩 GPU `ReadPixels` stall warning。
- 定向 44 项测试、生产构建和 `git diff --check` 通过；商业 DCC/atlas/状态包审计仍保持 RED。

## 10. 下一轮默认任务（更新）

1. 为预加载链增加失败、超时、取消和资源数量预算，避免城市规模扩大后初始化阻塞。
2. 对 GPU ReadPixels stall 定位截图/读回路径与目标设备帧率，建立真实性能证据。
3. 继续等待并验收 `main-pier` 真实 DCC、atlas、锚点和状态证据包。

## 2026-07-17 第一百八十轮：建筑资源预加载安全门禁

- 预加载入口新增资源清单去重、最大纹理数量预算、超时和 `AbortSignal` 取消；场景销毁或资源异常时回退到身份化程序化 Provider，不阻塞整页启动。
- `SimulationCanvas` 将预加载纳入初始化生命周期，避免已销毁场景继续挂起并在完成后写入 Pixi 场景。
- 新增构建清单、预算失败和取消回归；定向 47 项通过，生产构建与 diff 检查通过。
- 证据边界：当前只证明加载链路安全性；GPU `ReadPixels` stall、真实 DCC/atlas/状态包和目标设备性能仍未完成。
- 真实浏览器首次回归暴露生命周期调试记录会被后续模拟事件挤出最近窗口；已将该记录刷新限制在 debug fixture 分支，第二次真实浏览器回归稳定通过。

## 2026-07-18 第一百八十一轮：浏览器帧时间与 GPU 读回归因

- 浏览器 E2E 现在采集 1 秒真实 `requestAnimationFrame` 帧时间、P95、最大帧耗时和画布像素尺寸，并记录 WebGL `readPixels` 调用计数。
- 居民生命周期场景实测：45 帧/约 1.03 秒，平均帧耗时 23.48ms，P95 34.7ms，最大 67.6ms，画布 1366×768。
- 项目代码自身 `readPixels` 调用计数为 0；GPU stall warning 来自浏览器/驱动合成层，不能据此宣称项目达到目标设备性能。
- 证据边界：当前是 headless 浏览器基准，不代表桌面、移动端和嵌入 Q 音乐容器的最终帧率。

## 12. 下一轮默认任务（更新）

1. 对 DynamicScene 的每帧同步、对象池和动态建筑动画做分段耗时采样，降低 P95 长帧。
2. 建立桌面 GPU、软件渲染和目标嵌入容器三组性能基线及阈值。
3. 接入并审计 `main-pier` 真实 DCC、atlas、锚点和状态包。

## 11. 下一轮默认任务（更新）

1. 定位 GPU `ReadPixels` stall 的具体读回路径，建立真实浏览器帧时间采样。
2. 接入并审计 `main-pier` 真实 DCC、atlas、锚点与状态包。
3. 将预加载预算门禁扩展到全量建筑目录和生产包发布检查。

## 2026-07-18 第一百八十二轮：DynamicScene 分段性能采样

- `DynamicScene` 增加按查询参数开启的同步阶段采样，不影响生产默认路径；分别记录地块、建筑、居民、掉落物、清理和排序耗时。
- 浏览器居民生命周期场景实测 107 次场景同步：总耗时平均 0.368ms、P95 0.500ms；其中建筑阶段平均 0.237ms、P95 0.300ms，居民阶段平均 0.042ms、P95 0.100ms。
- 同一轮真实 rAF 基线为 54 帧/约 1.02 秒，平均 19.15ms，P95 33.6ms，最大 34.7ms；应用层 `readPixels` 仍为 0。
- 结论：当前 JS 场景同步不是该场景长帧的主要瓶颈，但 headless 帧率仍不足以作为商业级性能通过；需要多环境基线和真实 DCC 资源接入后的重复测试。

## 12. 下一轮默认任务（更新）

1. 建立桌面 GPU、软件渲染和目标嵌入容器三组性能基线及阈值。
2. 接入并审计 `main-pier` 真实 DCC、atlas、锚点和状态包。
3. 在真实资源接入后补做动画、粒子、显存和长时间运行基准。

## 2026-07-18 第一百八十五轮：静态视觉失效优化与回归

- `BuildingVisual` 增加建筑主体、稳定美术精灵和空闲占位几何的签名缓存；未发生等级、状态或资源变化时，不再每帧重复清空和重建静态图形。施工、水波、灯火、炊烟和居民动画仍保留动态更新。
- 修复缓存命中时占位层可能重新显示的边界问题，保证真实美术精灵与灰盒占位层的互斥关系在每次同步后成立。
- 优化后重复三环境采样仍出现明显 headless/驱动波动：桌面 GPU 平均 36.96ms/P95 51.9ms/最大 98.1ms，软件渲染 29.05ms/35.2ms/35.4ms，嵌入容器代理 35.63ms/50.1ms/50.8ms；DynamicScene 同步 P95 为 0.5/0.8/0.5ms，应用层 `readPixels` 仍为 0。
- 结论：静态失效优化已落地且功能回归通过，但本轮没有证明帧时间改善；性能门禁继续保持 RED，后续必须拆分纹理上传、渲染提交、动画/粒子与浏览器合成层，并使用重复采样的中位数/离群值报告。

## 13. 下一轮默认任务（更新）

1. 将三环境基线改为重复采样并输出中位数、P95 与离群值，降低单次 headless 运行噪声。
2. 增加渲染提交、纹理上传、动画/粒子和合成层的可归因 profile，继续关闭商业性能红线前的未知项。
3. 接入 `main-pier` 真实 DCC、atlas、锚点与七状态证据包；未有美术机产出前不宣称真实建模完成。

## 2026-07-18 第一百八十六轮：重复基线统计契约

- `qa:performance-baseline` 默认将每个环境运行 3 次，保留 `rawSamples`，并以帧时间中位数评估、以 `readPixels` 最差值守门；可用 `PERF_BASELINE_REPEATS` 调整重复次数。
- 新增聚合契约测试，验证异常长帧不会被平均值掩盖，且像素读回不会因取中位数而被隐藏。
- 本轮定向 18 项、全量 55 个测试文件/350 个测试通过；构建通过，2361 个模块；`git diff --check` 通过。
- 本轮未重新执行 9 次真实浏览器矩阵，因此没有新的性能通过结论；上一轮三环境帧时间门禁仍保持 RED。

## 2026-07-19 第一百八十七轮：三环境重复性能实测

- 完成 3 个环境各 3 次真实 Chromium 采样，场景功能和应用层 `readPixels=0` 全部通过，DynamicScene 同步 P95 为桌面 0.6ms、软件 1.1ms、嵌入代理 1.4ms。
- 稳健聚合后的帧时间仍全部超标：桌面 GPU 平均/P95/最大 42.37/66.6/68.2ms；软件渲染 42.36/64.8/66.6ms；嵌入容器代理 53.57/67.1/67.1ms。
- 结果说明当前居民生命周期垂直切片的 JS 场景同步不是主要瓶颈；GPU stall 警告仍出现，但应用层没有主动 `readPixels`。需要进一步对 Pixi 渲染提交、纹理/画布上传与浏览器合成层做可归因实验。
- 性能门禁继续 RED；本轮只增强证据，不代表商业级性能通过。

## 2026-07-19 第一百八十八轮：Pixi 渲染提交归因采样

- `renderProfile=1` 场景新增 Pixi `renderer.render` CPU 调用采样，与 DynamicScene 同步阶段分开记录；生产默认路径不启用该采样。
- 桌面 GPU 单场景验证：DynamicScene 总同步 P95 0.8ms；Pixi renderer.render 平均 1.818ms、P95 4.3ms、最大 62.1ms；场景功能通过，应用层 `readPixels=0`。
- 结论：已确认业务同步阶段很小，但单次 renderer 最大耗时存在长尾；该数据还不足以证明 GPU/驱动/合成层根因，需纳入下一次多环境聚合并与真实 DCC 资源对照。

## 2026-07-20 第一百八十九轮：渲染差分开关与真实运行时验证

- 新增 `parseRenderDiagnostics` 与浏览器运行时配置回传；通过 `disableArtwork=1`、`disableAnimation=1`、`disableTerrain=1` 可分别关闭 authored artwork、图集动画和地形层，默认生产 URL 全部开启。
- 浏览器 runner 支持 `BROWSER_E2E_RENDER_QUERY`，真实桌面 GPU 场景验证返回 `authoredArtwork=false`、`authoredAnimation=false`、`terrain=true`，证明差分参数已进入运行时。
- 对照运行：DynamicScene 总同步 P95 0.6ms；renderer.render 平均 1.732ms/P95 2.4ms/最大 74.3ms；应用层 `readPixels=0`，功能场景通过。该次是禁用贴图和动画的单场景样本，不能与上一轮单次全量样本直接推导收益。
- 全量回归 55 个测试文件、352 个测试通过；构建通过，2362 个模块；`git diff --check` 通过。性能门禁继续 RED，下一步将使用同一环境、同一场景、多个开关进行成组采样。

- 同轮新增四模式差分执行器 `qa:render-ablation`，支持按环境、模式和重复次数运行并以中位数汇总；当前仅完成工具构建验证，正式矩阵尚未形成可采信的数字。

## 2026-07-21 第一百九十轮：建筑贴图按需预加载优化

- 将建筑原画预加载从“当前建筑类型的全部 9 个等级”改为“当前快照实际出现的等级”；升级到未加载等级时仍通过 Provider 懒加载，不改变视觉降级边界。
- 真实桌面 GPU 方向性复测：完整渲染单次平均/P95/最大帧耗时 24.81/33.6/49.8ms；禁用地形单次 16.67/17.6/17.8ms；禁用动画单次 19.19/33.4/34.3ms；场景功能与配置回传均通过，readPixels=0。
- 上一组两次差分样本中，完整模式中位数 42.37ms，禁用建筑原画 17.84ms；样本受 headless 驱动波动影响，说明 authored artwork 仍是高优先级嫌疑项，但不能把方向性结果当作商业性能通过。
- 新增按等级预加载契约测试；全量回归、构建和 diff 检查通过。性能门禁仍 RED，下一步继续做纹理尺寸/上传和真实 DCC 资源预算审计。

## 2026-07-21 第一百九十一轮：运行时资源预算门禁

- 新增 `asset:runtime-artwork:budget`，对单张纹理、单建筑九级包、全量下载体积和解码后 RGBA 显存进行独立审计。
- 当前事实：28 个建筑包、252 张 PNG，总下载体积 125,331,330 bytes（约 119.5 MiB），估算解码内存 264,241,152 bytes（约 252 MiB）。全量下载目标 96 MiB，预算门禁为 RED。
- 单张纹理和单建筑包未触发本轮门禁；运行时已按当前等级懒加载，因此全量目录体积不等于首屏下载，但仍需 WebP/atlas/LOD 发行优化。
- 构建和 diff 检查通过；商业美术资源与性能门禁继续未完成。

## 2026-07-21 第一百九十二轮：384px 运行时发行包

- 从保留的 512px 源 PNG 生成 `public/assets/buildings-runtime-384`，28 类建筑、252 张等级图作为浏览器运行时发行层；源资产不被覆盖。
- `BuildingArtworkProvider` 已切换到 384px 运行时路径，浏览器仍保留现有按当前等级预加载和缺失等级懒加载逻辑。
- 运行时发行包审计通过：252 张、全部 384×384 RGBA、字节级唯一；下载体积 51,926,857 bytes（约 49.5 MiB），估算解码 RGBA 148,635,648 bytes（约 141.8 MiB），低于本轮 96 MiB 下载和 256 MiB 解码预算。
- 原始 512px 资产目录仍约 119.5 MiB，作为源素材包不计入首屏发行预算；真实 DCC/atlas/粒子包仍未交付，不能据此宣称商业美术完成。
- 真实桌面 GPU 浏览器回归通过：场景功能、5 建筑、居民/运输实体和配置回传正常，应用层 `readPixels=0`；本次 rAF 平均/P95/最大 36.34/50.9/66ms，性能门禁仍 RED。

## 2026-07-21 第一百九十三轮：运行时派生包流水线化

- 新增 `asset:runtime-artwork:build`，以 512px 源 PNG 批量生成 384px 运行时发行图，并写入 `runtime-artwork-manifest.json`，记录源 SHA-256、源/产物体积、尺寸和生成器。
- 运行时完整性审计现在同时校验派生包 provenance manifest、252 张九级覆盖、384×384 RGBA 和唯一性；预算审计继续通过 51,926,857 bytes 下载、148,635,648 bytes 解码内存。
- 当前仍没有 Blender 可执行环境，因此没有虚构 `.blend`、DCC 动画或真实 atlas 交付；派生包流水线只解决发行工程，不替代商业美术生产。

## 2026-07-18 第一百八十三轮：居民动态实体浏览器证据

- `SceneSyncPerformanceProfile` 现在同时记录建筑、居民、运输、掉落物、可见实体和对象池数量；浏览器验收不再只依赖页面文案。
- 真实 `civilization-resident-timeline` 场景采集 88 次同步：稳定观察到 5 个建筑、3 个居民视觉实体、1 个运输实体、1 个掉落物、14 个可见实体和 63 个池化实体。
- 同轮 rAF 基线为 50 帧/约 1.02 秒，平均 20.75ms，P95 33.4ms，最大 33.4ms，画布 1366×768；应用层 `readPixels` 仍为 0。
- 证据结论：居民入住前后场景实体已能被真实动态场景创建并稳定纳入可见/池化统计；本轮没有证明 500 户、300 栋、150 可见实体的生产规模，也没有证明实体在高频创建/回收压力下的性能。
- 商业资源包审计仍保持 RED：真实 DCC、图集、锚点和状态证据包尚未交付；浏览器/驱动 GPU stall warning 仍需多环境复测。
- 本轮最终验证：54 个测试文件、346 个测试全部通过，`git diff --check` 通过；构建结果沿用本轮 2361 模块成功记录。

## 2026-07-18 第一百八十四轮：三环境性能基线矩阵

- 新增 `npm run qa:performance-baseline`，按统一阈值顺序采样桌面 GPU、软件渲染和嵌入容器代理三种环境；默认报告红线但不伪造通过，`PERF_BASELINE_STRICT=1` 才会以门禁失败退出。
- 三组居民生命周期场景功能均通过，应用层 `readPixels` 均为 0，DynamicScene 同步 P95 均小于 1.1ms。
- 帧时间结果：桌面 GPU平均 19.90ms/P95 33.4ms；软件渲染平均 30.41ms/P95 48.6ms；嵌入容器代理平均 31.34ms/P95 50ms、最大 65.8ms。统一目标为平均不高于 16.7ms、P95 不高于 25ms、最大不高于 50ms，因此三组均未通过帧时间门禁。
- 结论：性能矩阵已经具备可重复证据，但商业级性能仍为 RED；下一轮必须降低渲染提交/合成长帧，并在真实 DCC/粒子资源接入后重测。
- 本轮最终全量回归：55 个测试文件、349 个测试全部通过；新增性能契约测试已纳入总回归，`git diff --check` 通过。

## 2026-07-21 第一百九十四轮：文明规模压力场景

- 新增 `qa:civilization-scale`：实际构造并验证 500 户家庭、300 栋建筑、150 个初始移动实体；DynamicScene 连续两次同步均保持 300 栋、135 个居民实体、15 个运输实体、450 个可见动态实体，无实体丢失。
- `qa:civilization-long-run` 通过 7,200 tick（30 天）长跑：三层快照均保持 500 户、300 栋；人口 1,750；物流效率约 99.99%；所有数值字段有效；最终建筑阻塞 172/300，说明系统确实暴露运营压力而非静态演示。
- 当前证据首次覆盖“目标规模的模拟状态 + 动态场景同步”两条链路，但仍不是商业级性能通过：长跑最终动态 agent 表为 226（压力运输实体会动态生成/回收），真实浏览器仍需接入 300 栋场景并重新测帧时间。

## 2026-07-21 第一百九十五轮：真实浏览器文明规模验收

- 新增 `civilization-scale` 浏览器压力夹具：使用 500 户、300 栋建筑、150 个初始移动实体，并将压力实体固定在首屏安全视口，排除相机漂移对可见性验收的干扰。
- 真实桌面 GPU 浏览器验收通过规模与功能门禁：300 buildings、135 residents、15 transport；visible entity 335–351；无 console error；应用层 `readPixels=0`。
- 同次真实浏览器性能仍为 RED：rAF 平均/P95/最大 118.52/166.4/166.4ms；DynamicScene 总同步平均/P95 7.66/10.9ms；renderer 平均/P95/最大 15.09/17.2/117.7ms；浏览器仍报告 GPU stall due to ReadPixels 警告。
- 结论：目标规模已经进入真实 Pixi 浏览器运行时，但当前实现不能宣称商业级流畅度。下一轮必须优先处理 300 栋场景的渲染提交、纹理上传/合成和长尾帧，并在真实 DCC/atlas/动画资源接入后重测。

## 2026-07-21 第一百九十六轮：动态场景重复同步缓存

- `DynamicScene` 增加快照/视觉引用/相机键缓存：模拟时钟未变化时不重复执行 300 栋建筑的 `Graphics.clear()` 和路径重建；相机移动只刷新可见性，快照边界仍完整更新。
- 定向渲染与规模测试 16 项通过，构建通过；兼容测试中对同一快照的原地状态修改和 tick 变化仍能触发重绘。
- 同一真实桌面 GPU 压力场景方向性复测：DynamicScene 平均/P95 2.78/12.6ms（上一轮 7.66/10.9ms）；renderer 平均/P95/最大 6.885/18.4/123.9ms（上一轮 15.09/17.2/117.7ms）。
- rAF 平均/P95/最大 129.14/216.7/216.7ms，受浏览器/GPU 长尾影响反而更差；因此该优化只确认减少了应用层重复工作，不构成商业性能通过，必须继续做 GPU/合成层差分。
- 下一轮：补真实浏览器规模场景或可复现的浏览器压力夹具，并将 300 栋、150 可见实体纳入性能矩阵；同时继续等待真实 DCC/atlas/锚点/动画资产，不能把压力灰盒当作商业成品。

## 2026-07-21 第一百九十七轮：抗锯齿与分辨率合成差分

- 渲染诊断新增 `disableAntialias=1` 与 `resolution=1`，仅 QA URL 生效；生产默认仍保持抗锯齿和设备分辨率上限 2。
- 同一 `civilization-scale` 真实桌面 GPU 复测通过实体门禁：300 buildings、135 residents、15 transport、visible 331–348；`readPixels=0`，但浏览器仍报告 GPU stall due to ReadPixels。
- 关闭抗锯齿并锁定 1x 分辨率后：DynamicScene 平均/P95 2.236/11.8ms，renderer 平均/P95/最大 5.26/18.1/114.7ms，rAF 平均/P95/最大 54.35/83.3/83.3ms；相较上一轮方向性改善明显，但仍未达到 16.7ms 帧时间门禁。
- 结论：像素分辨率/抗锯齿确实参与长尾瓶颈，但不能直接把正式画质降为 1x；后续需要在保持画质的前提下继续做纹理图集、静态层缓存、动画提交与浏览器合成差分。
- `npx vitest run src/rendering/renderDiagnostics.test.ts src/rendering/DynamicScene.test.ts` 16 项通过；`npm run build`、`npm test -- --run`、`git diff --check` 通过。

## 2026-07-21 第一百九十八轮：建筑静态缓存实验否证

- 将建筑主体与原画 Sprite 从状态动画层分离，新增 `staticBuildingCache=1` 诊断开关；状态徽标、施工、居民和环境动效仍保持独立动态层。
- 正式原画/动画/地形全开、300 栋压力场景下，开启缓存后的 rAF 平均/P95/最大为 122.26/166.4/166.4ms，renderer 平均/P95/最大为 8.953/31.8/160ms，较无缓存基线 91.63/133.3/133.3ms 与 5.889/18.2/114.2ms 更差。
- 结论：当前 Pixi/WebGL 环境中 300 个独立缓存纹理的生成和合成成本超过收益；该开关保留用于复现，不进入正式默认配置。下一步转向共享图集、纹理批次和可见区域资源策略。
- 全量 `npm test -- --run` 通过：57 个测试文件、355 个测试；已知 jsdom Canvas warning 不影响退出码。

## 2026-07-21 第一百九十九轮：建筑共享图集接入与真实浏览器差分

- 真实生成 `public/assets/buildings-runtime-atlas/`：28 张 1152×1152 atlas，包含 28 类建筑的 9 级、共 252 个 384×384 精确帧；manifest 记录列、行、帧矩形和 schema 版本。
- 运行时新增 atlas provider 和 `buildingAtlas=1` 诊断开关；生产默认仍使用独立 PNG，避免未经完整矩阵验证就改变发行行为。
- 真实桌面 GPU 单次对比：独立 PNG rAF 平均/P95/最大 41.36/66/83.3ms、renderer 1.714/3/74ms；atlas 为 37.66/50/50.2ms、1.497/3/69.3ms。方向性改善但均未达到商业目标，GPU stall 警告仍在。
- 资产审计、预算审计、构建、定向测试和 `git diff --check` 通过。下一轮扩大重复采样并接入可见区域/LOD 策略，再决定是否切换正式默认。

## 2026-07-22 第二百轮：共享 Atlas 正式默认切换

- `RenderDiagnosticsConfig.buildingAtlas` 改为默认开启，生产运行时通过共享 atlas provider 加载建筑帧；`disableAtlas=1` 保留为回归对照和紧急降级开关。
- 共享 atlas 与独立 PNG 使用相同的 384px 九级图像帧，不改变建筑内容、等级表现或动画层；缺失 manifest/加载失败仍回退到独立 PNG provider。
- `npm run build`、建筑 artwork/diagnostics 定向测试 9 项和 `git diff --check` 通过。商业性能门禁仍 RED，多次浏览器差分执行器本轮出现子进程无输出，未采纳为证据。
- 下一轮必须修复差分执行器的多次/多环境生命周期问题，再补软件渲染、嵌入容器和 300 栋压力场景的 Atlas 矩阵；同时推进可见区域 LOD 与资源上传控制。

## 2026-07-22 第二百零一轮：WebP Atlas 发行优化

- 运行时共享 atlas 改为 WebP 输出：28 张 1152×1152 atlas、252 个等级帧，总体积约 16.80 MiB；相较 PNG atlas 约 52 MiB，下载体积下降约 68%。
- 新增 `runtime-artwork-atlas-audit`，校验 manifest、28 类建筑、9 级帧、扩展名和实际文件数量；审计通过。运行时默认路径同步切换至 WebP，加载失败仍回退独立 PNG。
- 真实桌面 GPU 默认 WebP 场景回归通过实体与功能门禁：5 建筑、3 居民、1 运输、14 可见实体、readPixels=0；rAF 平均/P95/最大 32.79/49.3/66.7ms，renderer 1.15/2.2/69.6ms，商业帧率门禁仍 RED。
- PNG 独立降级路径在本轮同环境下未稳定返回可采纳结果，不能据此宣称降级路径性能；下一轮继续修复差分 runner 生命周期，再做完整多环境矩阵和视口 LOD。

## 2026-07-23 第二百零二轮：渲染差分生命周期与三环境矩阵

- `run-render-ablation` 改用 `close` 等待子进程输出管道结束，并以独立进程组清理超时子树；浏览器 runner 对 Vite 服务和浏览器关闭增加 1 秒强制清理兜底，端口不会由上一轮残留进程占用。
- 桌面 GPU `full/no-atlas × 2`、软件渲染 5 模式 × 1、嵌入容器 5 模式 × 1 均稳定返回 JSON；所有场景功能、实体和 readPixels=0 门禁通过。
- 垂直切片矩阵观测 rAF 约 16.65–16.68ms，renderer 平均约 0.34–0.75ms；由于场景实体量小且受 60Hz 调度影响，只能证明 runner 和基础渲染路径稳定，不能证明 300 栋商业性能。
- 生命周期任务可关闭；下一轮必须把同一矩阵接到 `civilization-scale` 目标规模，并补 WebP/PNG、视口 LOD、纹理上传和 GPU stall 的目标设备证据。

## 2026-07-23 第二百零三轮：目标规模全画质差分验收

- 修正 `civilization-scale` 压力夹具：默认 `full` 不再隐式关闭 authored artwork、authored animation 和 terrain；差分模式通过 URL 显式关闭对应层，并新增契约测试防止回归。
- 桌面 GPU 目标规模 `full`：300 栋建筑、135 名居民、15 个运输实体、至少 150 个可见实体，功能与 readPixels=0 门禁通过；rAF 平均/P95/最大 41.33/66.6/66.6ms，renderer 3.783/10.6/97.4ms，render sync P95 6.1ms。
- 同环境 `no-atlas`：rAF 46.39/66.8/66.9ms，renderer 3.828/10.5/102.7ms；`no-artwork` 50.83/83/83ms，`no-animation` 42.39/66.7/67.1ms，`no-terrain` 32.79/50/50ms，全部功能和 readPixels=0 门禁通过。
- 结论：目标规模全画质已获得真实有效样本，但商业 60fps 门禁仍 RED；当前最大瓶颈偏向浏览器合成/资源提交长尾，下一轮必须进入视口裁剪、纹理上传节流和 GPU 合成专项，不能以“可运行”宣称商业级流畅。
