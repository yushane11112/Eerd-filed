/**
 * Archived archipelago prototype exports.
 *
 * This module is intentionally outside `src/components` so the old island
 * renderer is not mistaken for the commercial city-simulation UI path.
 * Use only for explicit legacy/QA entry points.
 */
export { IslandCanvas } from './IslandCanvas'
